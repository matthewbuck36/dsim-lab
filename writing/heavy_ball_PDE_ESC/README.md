# Heavy-Ball PDE ESC Writing

Written deliverables for the Heavy-Ball PDE ESC work live here, outside of
`ros2_ws`.

Current report:

```text
baseline_hb_report/
```

ROS scenario/config/code files remain under `ros2_ws`; report text, report
figures, run manifests used for writeups, and generated metrics live here.

