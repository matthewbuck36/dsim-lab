# R8 moving PDE evaluator timestamp bridge

ADOPTED prospectively2026-09-11UTC under the user's simulation correction
authorization. [C03](r7_visible_c03_handoff.md) reached the global region after
fill/escape/SEARCH and has complete continuous-acquisition and direction evidence.
Its frozen evaluator verdict remains failed: the public PDE event uses85.0s,
while the typed confirmation, snapshot and fill retain input support84.927s.
The inherited preceding-convergence comparison therefore rejects the local fill.
This is a private evaluator coordinate error, not a reason to change the algorithm.

## Existing owner correction

Extend only the selected moving-PDE evaluation path within `run_scenario.py`.
Keep the existing centroid/recurrent bridge unchanged and preserve stationary
PDE and other legacy paths. Select this new private bridge only for the current
`v2_method_development_v1` and `m4_pilot_v10` moving PDE suites. Older M4
versions remain unselected; any future V11 registration is separate. Reuse the existing live StageA
refresh and final bag-outcome boundaries, collecting their required typed
DetectorConfirmation, legacy convergence mirror and immutable Timekeeper inputs
through existing monitor/reader owners. No new recorder, node, topic or pipeline.

Bind a unique typed confirmation to the public event using its exact eight
legacy values, selected metric, run/stream/frame/origin and valid positive
epoch/sequence/history coordinates. Validate the typed envelope and source equals
history end; preserve the existing0.5s publication bound. Use the canonical legacy
mirror to verify the original public PDE timestamp and eight-value payload.
Reject nonfinite/malformed values, duplicate-name conflicts, ambiguous matches,
conflicting typed identities, wrong scope and missing final joins. Repeated
identical observations may deduplicate without refreshing clocks. Follow existing
live pending/final error behavior for cross-topic arrival order.

The public event stamp, source timestamp, payload and digest remain unchanged.
Only a detached evaluator copy receives `(typed.source_stamp-origin)` for local
fill association. Preserve event/bag ordering, original detector latency, geometry,
cardinality, safety, ownership, source units and arrival threshold. Record both
original and adapted coordinates and exact binding hashes. Never clamp timestamps,
select nearest geometry, add tolerance or alter controller/algorithm decisions.

## Bounded validation and retained reassessment

Archive C03 source/evidence before production edits. Use its cached public event,
typed confirmation and legacy mirror as a small actual-message regression.
Verify baseline rejection before applying the bridge, successful exact join after,
live/final integration, pending order and meaningful adversarial identity cases.
Run one named focused source bundle: new main/adversarial tests plus existing
centroid event, arrival evaluator and alignment regressions,120s work within140s
total. Bind source/helper/test hashes and installed owners before/after. No full
simulation, numerical model or native analysis rerun belongs to this milestone.

After validation, perform one separately versioned cached C03 reassessment under
30s work/40s total. Rehydrate only the needed exported messages and original
readiness/bag/ROS coordinates from native odometry CSV. Invoke the same bridge,
`_staged_recovery_evidence` and `_post_recovery_global_proximity` owners. Save
original-versus-corrected component outcomes, public payload equality, source/input
hashes and exact noninterpolated first arrival, closest and final distance when
available. No raw bag scan, new reference solve or replacement trajectory.

This is reassessment of StageA/cardinality/arrival components. The exports do not
include every supervisor-command/contact input needed to rerun full `_bag_outcomes`;
retain the original complete ownership/safety/native proofs and that limitation.
Do not invent a live arrival stop, replace the original StageA timeout, modify any
frozen scenario/result/analysis/reference artifact or call this an untouched trial.
The original C03 verdict stays failed alongside the corrected sample evidence.

Source validation, independent review and a material checkpoint precede considering
the V11 draft. No further visible run is required solely to obtain GOAL_HOLD or
replace the already observed arrival. Full comparison still needs separate adoption,
fresh version registration, usable four-arm development analysis and confirmation
freeze/release. Simulation only; no physical/Pi/snapshot/V1/commit/push changes.
