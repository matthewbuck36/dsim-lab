# Q2 moving-cycle runtime source handoff

The selected policy is implemented and its source validation passes. Select
`v2_direction_policy=moving_cycle_coherence_v1` with robust rolling simulation.
It combines75% current covered-cycle mean and25% instantaneous world vector,
requiring current coherence lower bound>=.25 and three covered warmup cycles.
The old selectable `three_cycle_v1` remains the default with its original
numerical/weak-vector behavior. Source clocks, controller limits, cost signs,
moving authorization and old wire layouts are preserved.

The rolling owner caches the bounded piecewise-linear norm calculation; the
adapter publishes an additive policy companion. Existing scenario, recorder,
validator and analyzer owners bind explicit policy metadata and exact first
publications, including numerical support, state and source integrity. Old
absent-policy evidence remains under old strict semantics. No duplicate node,
recorder, evaluator, control owner or physical source branch was introduced.

Validation:1167 held integrated checks pass in162.63s with426 source pins
unchanged, no failures/skips/errors and one inherited analytic sqrt warning.
Required-domain source/upstream3 and original-overlay binding23 checks passed
separately. The isolated three-package build and all focused policy, recording,
source-clock, controller and real DDS checks are retained. Exact commands,
hashes and failed-version history are in `validation/q2_policy_runtime.md`.

An exposed supervisor callback stall required an explicit correction to the
existing lifecycle/supervisor/Gaussian owners. Canonical observations are hashed
once; exact-schema structural copies preserve all detachment boundaries. The
first CDR timing version failed2x and stays failed. The new fixed timing passes
4.582815x at363 observations, maximum46.180069ms. Actual DDS preparation/activation
and departure cancellation pass with complete timer<=79.667692ms, original
readiness receipts and valid contexts through Gaussian admission. At4000
observations copy-path maximum615.394943ms remains a scaling limitation; no
general real-time guarantee is claimed. `validation/q2_snapshot_serialization.md`
records the106 ownership/97 independent checks and exact old-hash/field/alias
parity. The original1000-update direction-core job remains closed at maximum
0.950311ms and was not rerun.

Installed Q2 overlay is external `builds/q2_policy_runtime_v1/install`; source
Humble then its local_setup and append only extremum-seeking/src to PYTHONPATH.
Never prepend the ros_esc package root with stale tracked egg-info. Refreshed
binding verifies21 console entries, generated old/new interfaces and copy owners.
Compared with D3,17 earlier owners changed,539 remain unchanged and12 additive
policy/copy files are recorded. Controller, acquisition and numerical-reference
owners remain unchanged. The live status records the material closure archive.

This closes source work, not scientific qualification. The75% rule was nominated
on reused discovery trajectories. Old Q1/D1/D2/D3 results remain closed, old
confirmation stays sealed and M4 remains unreleased. Next adopt the reviewed
fresh Q2 qualification proposal with new IDs, explicit Q2 build, unchanged
detector/neighborhood gates and prospective observed-phase reference dispatch.
Only its declared nomination branch may unlock fresh confirmation scientific
reads; no historical confirmation becomes new evidence.

Before M4, resolve the recorded Arm B centroid/stationary adapter gap and audit
the separate composer future-clock result admission issue. Neither was exercised
as the cause of the corrected cancellation. The unchanged four-arm16-run pilot,
latency/direction/availability targets and full acceptance report remain pending.

Branch remains feature/gesc-gaussian-robustness-v2 at HEAD/origin3369cfc with
task-owned dirty source/evidence retained. No new commit/push, V1 change or
physical action occurred. The full approved implementation goal remains active.
