# M4v11 fresh arrival comparison plan

ADOPTED prospectively2026-09-11UTC after the completed R8 source and cached
component milestone. The filename is retained for navigation compatibility.
C01 remains closed behavioral failure and C02 closed startup failure. C03's
original frozen evaluator failure remains retained alongside its separately
validated cached component reassessment. No old outcome or slot is reused.
V10 remains [CLOSED_INCOMPLETE](m4_pilot_v10_handoff.md).

## Preconditions and scientific contract

Prerequisite evidence is now established by [C03](r7_visible_c03_handoff.md)
and [R8](r8_pde_evaluator_timestamp_handoff.md): complete recording/final zero/
cleanup, valid native lifecycle, actual continuous VERIFY/initial DESIGN,
passing selected24-target independent reference with4 eligible estimates, and
local fill/escape/restored SEARCH/global arrival. R8 corrects only the private
public/typed timestamp association, with147 passing checks and one cached
reassessment. First actual C03 arrival152.519s at0.499493m; nearest0.218300m.
Original C03FAIL and StageA timeout remain unchanged; no live arrival stop or
full bag-outcome rerun is claimed. These exposed cases support development,
not untouched confirmation or broad qualification.

Inherit the [V10 comparison contract](m4_v10_arrival_comparison_plan.md), including
all arrival, safety, recording, authority, motion, reference and usable-science
requirements. Freeze validated R5 self-fill handoff, R6 expiry handling, R7
pre-admission approach and R8 private evaluator corrections. Keep A/B stationary
and C/D rolling GESC with moving-cycle coherence and centered verification;
A/C use PDE, B/D recurrent detection. Preserve V6 half-gain controller, detector/
estimator settings, geometry, starts, costs, fills, escape and disturbances.
Explicitly select existing `controller_spawner_load_recovery_enabled:true` for
all V11 arms, as validated in38 existing startup checks and exercised by C03.
V10 serialization remains unchanged. This is an existing idempotent startup
path, with no new runtime mechanism. A/C measures the complete continuous
acquisition package. GOAL_HOLD/second ranking and escape bearing alignment
remain optional diagnostics; safety responses remain required.

Keep the original detector-latency estimand: all six independently eligible,
observed confirmation pairs (A/B and C/D in each of three conditions), at least
30% reduction of the enabled median relative to the control median, and no
additional wrong fills or goals. Preserve residence, censoring and negative
counts. Missing endpoints leave the target unavailable. Keep the existing
direction median/P90/availability thresholds and observed-phase stationary GESC
reference; neither the estimand nor its denominators change after observation.

## Fresh identity and minimal source registration

New identity: `m4-pilot-v11`, suite `m4_pilot_v11`, method
`recurrent_arrival_v11`, exclusive root
`/home/mattb/Experiments/GESC-Gaussian/v2/pilot/m4_pilot_v11/`.
Reserve four visible primary nominal development cases with seed 26091021, then
twelve secondary confirmation cases: nominal seed 26091022, Gaussian sensor
noise std 0.015 seed 26091023, sensor/pose delay 0.10 s seed 26091024. Every block
retains A/B/C/D order. These are fresh seeds on exposed geometries; the internal
`holdout` partition name conveys no unseen-condition claim. No replacement or
post-freeze tuning is allowed.

Use a minimal exact V10/V11 arrival-family predicate within existing owners;
keep historical defaults and V10 outputs unchanged. The six selection owners are:

- `m4_scenario.py`: exact registration, ownership, method/seeds/budgets and
  inherited arm, arrival, topology and matched-control bindings.
- `scenario_schema.py`: exact arrival-admission registration.
- `run_scenario.py`: exact evaluator, strict subreaper and shared-daemon routes.
- `m4_workflow.py`: existing source/runtime binding, prepare/verify, science
  receipts, release and report paths. Nested scientific-document checks use the
  selected contract version instead of a literal V10 identity.
- `m4_pilot.py`: existing scientific experiment/method fields and arrival-family
  selection; retain the historical scientific format `version=m4-pilot-v1`.
- `evaluate_m4.py`: existing arrival/motion, labels, references and summary
  selection. Do not change their calculations or completeness rules.

Reuse `run_m4.py`, `m4_science_job.py`, native decoder/validator and numerical
reference owners. No new pipeline, node, topic, interface or runtime mechanism
is introduced. Adding a version name alone is insufficient: V11 must receive the
arrival policy and budgets throughout, with no legacy fallback.

## Finite validation, freeze and release

For this source milestone, review the bounded source changes and run one named focused
version/selection/release bundle with relevant V10 compatibility checks: 230 s
work, 5 s termination allowance, 260 s inclusive wrapper maximum. Preserve any
failure and bind the source/runtime/helper population and all 21 installed
entry points before/after. Save validation, checkpoint and source archive, then
perform one exclusive preparation through existing owners under 600 s. Review
the resolved contract before dispatch.

Use the same verified `stationary_recurrent_pairing_v1/runtime_environment_v2.sh`
with clean PYTHONPATH, domain 201, localhost-only and DISPLAY `:0`. Bind the actual
Q5 console metadata/canonical modules and generated stationary/recurrent/guidance
interfaces, R5/R6/R7/R8 source validation and C03 acquisition/native/reference
receipts, the R8 cached reassessment, adopted plan and orchestration helpers.

Keep 720 s recorder, 900 s inclusive case, 45 s shutdown grace and 30 s independent
cleanup reserve. Per four-run block keep 240 s labels, two 45 s references and
10 s summary; freeze/report retain 40 s total, split 20/20. Total science remains
`4*(240+2*45+10)+40 = 1400 s`; suite maximum remains 15800 s. The external command
keeps SIGINT at 15680 s plus 120 s kill grace within that maximum. Preserve all
partial outcomes and original failed budgets.

Dispatch four visible development cases first. The inherited
`usable_four_arm_analysis_v1` gate must verify four COMPLETE usable scientific
products, successful labels/both references/summary jobs, exact 24 targets each
for C/D, all required checks and late nested hashes. At least B or D must show
valid local recovery through arrival; D must establish actual continuous
acquisition. Complete baseline failures, censoring and unexposed targets remain
valid science. All twelve confirmation slots must remain untouched until the
existing one-time release succeeds. An incomplete development acquisition or
failed development science gate closes the new version with confirmation withheld.

Retain and report all sixteen outcomes, analyzed versus unavailable metrics,
actual denominators and limitations. This plan and selected C03/R8 evidence cannot
establish broad qualification or close V2. Simulation only; no physical, Pi,
snapshot, V1, commit or push action is authorized here.

## Pre-implementation review refinements

Add V11 explicitly to R8's moving-PDE selector as well as existing recurrent,
arrival, subreaper and shared-daemon routes. Extend the exact arrival family
through analyzer and workflow release/report owners. Preserve historical
version rejection tests by replacing only their now-supported V11 sentinel;
remove positional assumptions that V10 is the last registered experiment.
Source tests use real inherited fixtures/routes and late nested receipt
checks. Existing240s labels allowance is a finite prospective cap, not a
measured guarantee for four longer recordings. Any development science failure
withholds confirmation; no fallback to the older clean-timeout release policy.

## V11 prospective latency measurement feasibility

Before releasing confirmation, additionally require two independently eligible,
observed nominal development first-opportunity pairs, A/B and C/D, using the
unchanged existing residence/latency/censoring owner. This is a V11-only
measurement feasibility condition, not a demand that development show30%
improvement or that baselines pass behavior. Keep complete censored results
as valid science, but withhold confirmation if either development contrast has
no observable latency pair. Record this explicit reason and all endpoints.
V10's release semantics remain unchanged. The final scientific target still
uses all six independently eligible observed confirmation pairs and original
median comparison, with no changed labels, endpoints or denominators.
