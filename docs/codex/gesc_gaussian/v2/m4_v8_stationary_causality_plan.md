# M4 stationary typed-event causality correction after V7

Status: ADOPTED FOR SIMULATION SOURCE IMPLEMENTATION, 2026-09-10 UTC.
The active user goal authorizes recommended bounded corrections. This source
milestone precedes any separately adopted fresh V8 comparison. It does not
admit V8 experiment routes, preparation, acquisition or resumed V7 slots.

## Verified prerequisite and defect

V7 is closed INCOMPLETE: A COMPLETE/integrityPASS/behaviorFAIL; B INCOMPLETE;
14UNSTARTED, no science or holdout release. Read m4_pilot_v7_handoff.md and
validation/m4_pilot_v7_result.md. Closure audit SHA256
 e5eac2ddd288d15600b2b7c17778b9e08bf1a858d72393fb571429a9b2e9bf9f;
material closure manifest at checkpoints/m4_v7_closed_incomplete_v1/manifest.json
under /home/mattb/Experiments/GESC-Gaussian/v2/, SHA256
 ea3fa730800fc55d7d07878b2794c9c39e578a113b80e876898f65061d4f1359.
The verified tar contains400 repository files,860external refs,1571813bytes;
the two raw recordings remain external hash references, unchanged.

B's original completeness SHA256
5e25abcf2a926075f0ffffa0c4ef2cedf69b052d62bd19ea7016d550f36cc798
has51/52passing checks. Its stationary_centroid_contract already associates
one typed request at source353.90000000000003s with activefill1 and matching
type20 event published354.0s. Its later algorithm_event_source_causality check
incorrectly uses only the legacy /fill_requests topic, which intentionally has0
messages. The typed request topic has1. Source and the existing typed audit
establish this routing defect without post-close bag decoding.

Both runs hit the unchanged360s Stage A limit without a later SEARCH boundary.
Repairing recording integrity does not establish behavioral or scientific
acceptance. The original V7 report remains1COMPLETE/1INCOMPLETE/14UNSTARTED,
all192targets retained and both research goals EVIDENCE_UNAVAILABLE.

## Bounded implementation and ownership

Use builds/m4_v8_stationary_causality_v1 for all fresh helpers/receipts. Preserve
exact validator, workflow, relevant fixture sources and original source map
before edits, plus the V7 B small-file/bag references from the closure manifest.
Do not overwrite any old source archive, helper, report or recording.

Extend validate_run.py only for runtime validation selection:

1. Retain a tri-state stationary causality authority from the existing
   stationary_centroid_stream_errors invocation: unselected, validated with no
   errors, or failed/unavailable. Reuse the existing typed validator unchanged.
2. Keep moving lifecycle authority first. For selected stationary centroid mode,
   use its recorded typed authority before the legacy robust-profile fallback.
   Both centroid_windows_v2 and centroid_two_block_v2 must use the same typed
   protocol. Invalid selected configuration, malformed/missing authority or
   a caught authority exception must fail closed.
3. Preserve fill_event_source_causality and its exact legacy source matching,
   all legacy producer semantics, moving lifecycle handling, timestamps/units,
   clock extrema optimization, readiness, report schema and final-zero gates.
   Never synthesize a legacy request, merge unrelated request protocols, skip
   causality, suppress typed errors or weaken typed identity/order checks.

Add this plan to m4_workflow.collect_sources as a provenance-only one-line
addition; no experiment selection or scientific method changes there.
New full-validator tests belong in test_m4_v8_stationary_causality.py and reuse
existing Q5 recorded-wire fixtures. No duplicate recorder/validator pipeline,
ROS nodes, public messages, producers, tuning, topic, deadline or physical edits.

## Regression contract and finite commands

Before changing production, construct and independently review meaningful
full-validator fixtures that include the typed request, active fill and fill
AlgorithmEvent together. Existing Q5 full-validator coverage is request-only;
its separate DDS fixture proves typedrequest→fill but not final validation.
Exercise both centroid modes, no-request quiet selection, missing/corrupt typed
request or authority, source/origin/hash/identity and publication-order faults,
invalid selection/authority exceptions, and legacy/moving isolation. Reuse
existing typed validator fault fixtures; do not weaken their strict contracts.
A partial synthetic full-validator input must stay explicitly scoped rather
than be called a complete recording.

Preserve exact fixtures and command selection before each release. One baseline
under60s uses the new module against the unchanged validator; record expected
routing failures honestly. Then make the bounded source correction and obtain
independent review. One focused bundle under180s contains the new module,
test_q5_stationary_recording_contract.py and test_q5_stationary_fill_protocol.py.
One relevant bundle under240s contains test_experiment_recording.py,
test_v2_lifecycle_recording.py, test_v2_recording_contract.py,
test_q3_centroid_recording_contract.py, test_q7_recording_selection.py,
test_m4_v7_recording_clock_range.py and test_q5_stationary_fill_transport.py.
These cover legacy event matching, moving lifecycle, both detector selectors,
clock-range preservation and actual stationary DDS producer/consumer behavior.
No Gazebo E2E is selected; no unrelated passing matrix is rerun for context.

Use the existing corrected external source wrapper pattern: clean
Humble→Q2→Q5, append extremum-seeking/src, domain199, localhost1,DISPLAY:0,
PYTHONDONTWRITEBYTECODE=1. No other test/runtime graph may overlap. Include the
whole clean-environment command within each outer cap, with inner timeout and
bounded cleanup. Record exact unique JUnit identities, failures/skips/unidentified
rows, before/after source maps, selected environment and21 installed bindings.
Never count anonymous JUnit placeholders as passes. Compare source deltas with
the preserved V7 map; source integrity and independently reviewed fixture
coverage are required before a successful source gate. No automatic retry or
cap extension; preserve a failure before separately amended correction.

Run one actual run_scenario/record_run --help gate under60s on final bindings.
The source amendment does not change compiled interfaces, so do not rebuild
solely because Python source changed.

## One read-only retained recording verification

After source/CLI PASS and held helper review, run exactly one verification of
retained V7 B through validate_run_directory(write_report=False), under180s
inclusive. Derive exact original input paths, byte sizes and hashes from the actual
closure manifest before release. The
retained B raw bag is311218176bytes. Bind all original run inputs, old
completeness, selected source map, helper and command before/after the job.
No model evaluation, labels, direction reference, replay or acquisition occurs.

Require the new report's algorithm_event_source_causality to pass with no
errors, overall passed=True, and failures=[]. Allow only those two consequent
top-level fields and that causality check to differ from the original report;
every other check, typed audit, count, warning and report field must remain
exactly equal. The original52-check report, metadata, bag and every other input
must remain byte-identical. Save the newly returned report only under the fresh
build. This diagnoses/corrects the validator; V7 remains CLOSED_INCOMPLETE and
its slot is never replaced or reclassified.

## Closeout and continuation

Record exact results, context/diff checks, source handoff and material archive.
Preserve the completed1422-case V7 source evidence and its qualified mixed-map
scope; do not add those historic passes to a new current-source total.
Only after this source milestone closes, adopt fresh V8 identity/preparation
prerequisites separately using existing comparison owners. Keep all16 original
slots/192targets, controller and detector/direction settings, scientific method,
720recording/900case/15300suite budgets and one-time holdout gates unchanged
unless later direct evidence supports a separately saved bounded amendment.
No physical/Pi or V1 work is authorized. Both research goals remain open.
