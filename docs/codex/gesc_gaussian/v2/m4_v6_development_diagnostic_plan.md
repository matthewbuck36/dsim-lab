# M4 v6 bounded development diagnostic

Adopted2026-09-10UTC under the active full V2 goal and the user's explicit
authority for recommended corrections. M4v5 remains CLOSED_INCOMPLETE and
immutable. Its material closure archive SHA256 is
`e8d66d8f801f96a0223ef19aa2658f9fc73921f57a030d9b3fa759f09a334a12`.
No source edit, new preparation or acquisition is released by this diagnostic.

## Questions and existing source evidence

The original objectives remain faster settling/trapping detection and better
GESC directions while moving. Latest development evidence has three distinct
limitations: B has zero confirmations; C/D have confirmations but no accepted
moving snapshot; D also fails centroid publication coverage. Diagnose these
separately before committing another expensive experiment version.

Read-only source review already identifies inactive/waiting/stale centroid
publication silence against an unchanged readiness-wide1s coverage limit.
The likely source correction is an explicit selected invalid-status heartbeat
through the existing watchdog/publication owner. It must preserve SEARCH-only
integration, fresh-source admission, epoch/confirmation semantics, legacy
defaults and strict coverage validation. Exact implementation awaits a separate
amendment; no recorded gap endpoints or M3 rejection cause is invented now.

## One finite read-only extraction per closed development B/C/D

Use the existing `read_run_bag` and `records_for_alias` owners, once per original
closed run under `pilot/m4_pilot_v5/runs/2026-09-10/`. Original artifacts must
match `builds/m4_v5_moving_fill_v1/pilot_closure_audit_v1.json` (SHA256
`b4b7f13c5250ef8243e8c5a7205d38f2ee94c6cce899ae22cd72d464968bd109`).
All623 source pins from final v5 validation stay unchanged before/after/exit.
Hash each original run's files before/after/exit, including its closed bag;
never rewrite, reindex or reclassify the original.

For all three runs select algorithm_events, algorithm_state, recording_ready,
clock, timekeeper and pose. For C/D also select v2_search_epoch. The stationary
B route has no epoch-context publisher/subscription or bag topic, verified in
its resolved manifest before any read; retain that explicit not-published
applicability record rather than infer a missing moving stream or invent epochs.
The unchanged coverage owner maps
readiness bag boundaries through recorded /clock; Timekeeper's experiment
origin alone cannot reproduce that definition. This input clarification was
saved before any diagnostic read and adds no job, retry or budget.
For B/D also select
centroid_convergence_diagnostics. For C/D select typed detector confirmations,
v2_direction_diagnostics, v2_source_provenance, candidate snapshots and fill commands/results using
their actual aliases from the existing resolved manifest. Verify exact alias
definitions before any read; a missing required definition remains a diagnostic
failure. Optional candidate/transaction topics with zero recorded messages must
retain that zero count and their manifest/publisher/bag-topic applicability
metadata. Such expected zero-message routes are not fabricated data and must
not be confused with a missing required definition or a lost nonempty stream.
These applicability clarifications were saved before any diagnostic attempt;
they do not change a completeness rule or reclassify the original run.
The direction message already contains synchronized raw observations plus
first-publication state/stamp. Retain these full recorded payloads for a later
separately bounded existing-owner evidence diagnosis without another bag read.
The provenance stream also carries independent invalidation/revocation inputs
used by the moving evidence owner; retaining nested direction observations
alone would omit those callbacks. This clarification was saved before reads,
within the unchanged per-stream/total row limits and execution allowances.
Recorded publication/bag times still cannot reconstruct ROS/steady callback
receipt timing; no later replay may silently claim exact runtime causality.

Retain complete events, state transitions, centroid messages, pose, readiness,
epoch context and typed direction/confirmation/transaction payloads externally,
with original bag/publication/source timestamps and validity flags. Compressed
JSONL is permitted for volume; use explicit tagged NaN/Infinity serialization,
never silently replace unavailable values with zero or silently drop records.
Do not truncate observations: abort if any selected stream exceeds40000 rows
or the selected total exceeds160000 per run. Save counts and exact SHA256s.

The extractor may summarize only recorded values: event/transition reasons,
centroid reset/validity counts, finite metric score/radius ranges, score-only/
confinement-only/joint eligibility counts, and readiness-period centroid
publication gaps with adjacent timestamps/states. Preserve each gap, including
entry/exit boundaries under the existing validator's definition; do not weaken
the1s coverage threshold. B summaries must distinguish unready/invalid history
from valid history failing a metric or confinement check. C/D event/transition
summaries may report verification-deadline cancellation, but do not infer the
unrecorded last M3 evaluation reason or callback order from it.

No new detector replay, model/field/topology calculation, label/reference job,
scientific endpoint calculation, parameter sweep or empirical reclassification.
These are descriptive diagnostic extracts from a closed failed development
attempt, not accepted comparison evidence or holdout qualification.

## Bounds, validation and next action

External root: `builds/m4_v6_development_diagnostic_v1/`. Preserve exact helper
source, commands/environment, outputs and failures with exclusive filenames.
First validate the serializer and gap/summary handling using finite declared
fixtures in one45s allowance. Then one attempt per B/C/D, each60s execution
plus10s bounded exit/hash allowance (70s inclusive), no retries. Total255s
maximum across fixture/extractions. Use clean Humble→Q2→Q5 installed owners
without ROS initialization. No extra simulation or scientific budget renewal.

Keep all failed/partial extracts. If export fails, a corrected version needs a
separate recorded amendment; never overwrite or silently rerun. After the
extractions, save the exact diagnostic result and select the smallest justified
source correction or separately bounded offline evidence investigation.
The next method/source amendment must preserve all retained evidence and all
legacy selectors, explicitly distinguish diagnosis from research success, and
pass focused/actual integration tests before any fresh16-slot release.
No physical/Pi, V1, commit or push action.
