# R21 visible noisy D01 preparation appendix

Status: **ADOPTED**, 2026-09-11, before preparation or dispatch. Current
source-validation v2 passes79checks with547stablepins; independent retained
review v4 passes1496assertions. Earlier reviewer-format failures remain saved.
Concrete preparation must still pass before this one authorized visible case.

This appendix instantiates the one visible case in the adopted
[R21 plan](r21_recurrent_trapping_plan.md). The original source bundle passed
737 checks; four retained D nominations passed certificate/raw-quality checks and
produced four bounded proposals. These are source/component evidence, not live
activation, recovery, arrival or direction qualification.

## Exact case and compatibility amendment

Copy case index11 from the preserved `pilot/m4_pilot_v12/scenario.yaml`: secondary
two-light layout, start(0,0,0), Gaussian sensor noise0.015V, no source or pose
delay, recurrent detector, rolling GESC, moving-cycle coherence, centered
verification and the existing half-gain controller. Keep the frozen numerical
parameters, cost sign, raw-cost ranking, fills, escape/assist and safety settings.
Change only the explicit evidence policy to `recurrent_trapping_v1`, development
identity and finite budgets. Existing spawner recovery remains enabled.

Run/case ID: `v2_method_development_D_20260911_r21_01`; fresh seed260921001;
schema14, suite `v2_method_development_v1`, development partition. The input
topology and every reused V12 result are exposed development evidence. Template
hashes and the exact original case are retained in `template_receipts.json`.

Keep V12's eleven required predicates and both declared complete direct/assisted
recovery paths. `required_state_path` remains calculated and diagnostic. An
earlier SEARCH→VERIFY→SEARCH rejection must not veto a later valid completed
escape/recovery and arrival; all eleven other requirements still gate acceptance.
The prior schema permitted this only for the V12 suite. A separately reviewed
bounded correction extends that existing exception only to schema14 named
development, explicit `post_recovery_arrival_v1`, and `recurrent_trapping_v1`.
Other versions and policies keep their existing first-path behavior. Root owns
the schema/new-test application and separate source validation v2; external
before/proposed files, patch and hashes preserve the exact amendment.

Preparation requires original `focused_execution.json` as historical737-PASS
evidence; current `visible_source_validation_v2.json` with all current pins,
original receipt hash and the exact intentional schema delta/new test; and
`retained_component/result.json` COMPLETE with all six decision checks true,
four proposals, stable inputs and successful `retained_execution.json`.
It also checks the interface build, current generated wrappers and actual21
installed q5 entry-point bindings. The original source/component receipts are
never rewritten after the separately declared schema correction.

## Finite execution

One exclusive external case root:
`development/20260911/r21_recurrent_trapping_v1/visible_integrated_D_01/`.
Use parent `runtime_environment.sh`, clean initial PYTHONPATH, no RMW override,
ROS_DOMAIN_ID201, ROS_LOCALHOST_ONLY1 and DISPLAY:0. Existing Gazebo GUI and
`subreaper_group_v3` process owners remain selected. No build or retry.

Preparation:80s internal work,85s SIGINT plus5s kill,90s inclusive. Acquisition:
Stage A300 and post-stage300 simulation seconds; explicit simulation duration600s
and recorder wall duration600s; preflight45s; scenario wall650s and shutdown30s.
Root lifetime720s includes a30s independent cleanup reserve. The nested runner
receives root deadline−30s; the outer owner retains25s cleanup within the final
deadline. External SIGINT715s plus5s kill. All budgets are maxima; shared absolute
deadlines may stop a slow run earlier. Strict daemon baseline, recorder readiness,
final zero and both cleanup owners are required. Preserve every failed attempt.

The helper entry points are:

```text
prepare_attempt.py
run_attempt.py
analysis/run_analysis.py --run-directory <recorded-run-dir> --output-root <case-root>/analysis_v1 --expected-run-id v2_method_development_D_20260911_r21_01
analysis/run_direction_reference.py --analysis-root <case-root>/analysis_v1 --output-root <case-root>/direction_reference_v1
```

Each is run from the repository in the stated environment and corresponding
external timeout. Preparation does not launch a graph. Runtime alone uses the
existing scenario/recorder/process owners. Never dispatch from this document
without successful concrete preparation and root adoption.

## Measurements and decision

After terminal complete recording and passed cleanup, permit one native analysis:
110s internal work,115s SIGINT plus5s kill,120s inclusive. It captures one BagData
from the existing analyzer read and retains the native validator's second scan;
no additional bag read. Select D/noise/development explicitly, export the new
recurrent snapshot/command wrappers with original certificates, diagnostics,
states/events/guidance and all actual commands including zero pulses. Bind the
same frozen source and acquisition receipts. Missing evidence remains missing.

Use the unchanged motion metric after native lifecycle validation, and explicitly
select R12 `_arrival_metrics(..., binding_method='recorded_pose_arrival_v1')` to
join the exact recorded evaluator pose. Live arrival is a separate observed
sample; never substitute its timestamp. Arrival within0.5m suffices; GOAL_HOLD
is optional. Analysis completeness is separate from behavioral pass, observed
negative/censored arrival and measured stops.

Reference:38s internal work,40s SIGINT plus5s kill,45s inclusive, through the
retained normalized-input/evaluate_direction_targets owner. Keep all24 scheduled
targets at15+30k seconds, k=0..23, even beyond this600s maximum; later/unexposed,
unqualified, uninformative and unavailable targets remain explicit. Preserve the
original eligibility and observed-phase stationary-GESC reference meaning.
Existing direction thresholds stay median≤30°, P90≤60°, usable-averaging
availability≥0.8 among eligible informative targets; no eligible targets cannot
pass. Report instantaneous comparison and all denominators.

The selected case succeeds only with authentic continuous verification, committed
bounded fill, existing owned escape/recovery, actual global arrival and all
required evidence/safety/cleanup checks. Report direction quality independently;
verification success cannot qualify SEARCH direction. This case releases no new
matrix and makes no physical, global-robustness or universal minimum claim.
