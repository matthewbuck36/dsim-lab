# R3 arrival-based development attempt03

PROSPECTIVE, 2026-09-10. Preparation only until the existing-owner evaluator
correction and focused checks are complete and source is frozen. Authority is
the user's [arrival amendment](global_arrival_acceptance_20260910.md).
Attempt02 already demonstrates selected-case arrival; preserve its original
failed classification and separate user-revised arrival assessment.

## Hypothesis and informative difference

The same measured recurrent detector and centered moving verification should
again produce local verification/fill/escape and subsequent global arrival.
The corrected existing monitor should recognize the confirmed support/fill
coordinate, advance to the post-recovery stage, and stop at recorded arrival
without requiring a second detector event or GOAL_HOLD. This tests the actual
corrected end-to-end stop path and a second nominal development realization;
it is not a new algorithm calibration or untouched confirmation.

Use exposed nominal two-source geometry and unchanged numerical/runtime
configuration from02. New case/run `v2_method_development_D_20260910_03`, seed
26091002, exclusive external `development/20260910/visible_integrated_03/`.
Select explicit `success.criterion: post_recovery_arrival_v1`, with existing
local direct/assisted recovery paths ending in SEARCH, local event sequence,
recording/cleanup, unique fill/cardinality/command ownership and post-recovery
global proximity predicates. The declared global arrival radius remains0.5 m.
Do not select terminal-state, second-convergence or GOAL_REACHED predicates.
Retain their observation/reporting when they actually occur.

## Budget and decision

Reuse the02 finite GUI wrapper, selected schema2 environment, domain201/local
only, exact shared daemon baseline, subreaper_group_v3 and strict cleanup.
Keep180/120 simulated stage bounds,300 recorder-wall,340 process-wall,30 shutdown
and420 root-wall limits. Actual global proximity should stop the case sooner.
No concurrent Gazebo/test/profile job. Freeze selected source/scenario and save
exact argv before dispatch. No numerical method change or interface rebuild.

Require COMPLETE recording, VALID lifecycle, scoped cleanup, an actual local
moving verification/committed fill/escape and observed global arrival after
recovery. Confirm live and retained evaluator agreement on stage completion,
support timing and spatial arrival. Measure sensor-cycle eligibility and actual
motion through verification; retain every failure and any zero-command pulse.
Goal holding is not a success condition. A failed new attempt remains failed.

After terminal cleanup, use the existing prepared analysis helper under120 s
with its declared two native scans and shared in-memory exports. Later fixed
direction targets may be evaluated under45 s after qualified inputs are saved.
Keep new exposed-target denominators separate. No matrix/holdout is released by
this plan; full comparison still depends on its analysis/method gates.
