# Q5 Arm B source handoff

Status: CLOSED_SOURCE_VALIDATION_PASS. Scope: simulation-only source prerequisite
for the approved V2 comparison. Branch feature/gesc-gaussian-robustness-v2,
HEAD3369cfc83a64ff5d8354827fd5310caaf0c8e945; task changes remain uncommitted.
No physical workspace, hardware, V1 commit, numerical model or old study changed.

## Result and owners

Arm B now consumes the unchanged typed centroid confirmation through the existing
stationary Supervisor verification/ranking, emits StationaryFillRequest and uses
the existing Gaussian receipt-ended fit and legacy result/escape path. A remains
legacy stationary, C/D remain moving transactions. Exact schema, selectors,
receipt leases, clock origin, unique relative correlation, original deadlines
and CREATE/REDESIGN rules are in q5_stationary_centroid_adapter_plan.md.

Supervisor holds original diagnostic/receipt identity across verification and
redesign, with no fabricated PDE array or score-unit conversion. The Gaussian
adapter freezes original receipt-ended pose/cost tuples before waits, shares the
original estimator/designer/registry path, permits bounded cross-topic state
ordering and checks delivered authority immediately at staged registry mutation.
Exact retries replay cached outcomes without refit/recommit. Redesign retains
its old no-candidate-amplitude-floor policy. Lightweight live authority uses a
separate callback group on the existing executor; no worker/node was introduced.

The shared centroid arithmetic was moved unchanged to centroid_contract.py and
re-exported by validate_run. Selected launch parameters, topic manifest, existing
recorder/validator/analyzer and the new common stationary recorded audit preserve
identity/result joins. The existing generic bag reader already supports the new
CDR type; it does not invent absent legacy source-valid flags. Supplemental
publication intervals retain historical goal convergence_time and scope limits.

## Validation and recovery

Exact commands, source/test pins, baseline attempts and every outcome are in
validation/q5_stationary_centroid_adapter.md and external
/home/mattb/Experiments/GESC-Gaussian/v2/builds/q5_stationary_centroid_adapter_v1/.
Definitive pre-edit typed-consumer absence:1expectedFAIL0.34s. Final independent
210PASS15.81s; recording40PASS1.72s and appended sqlite/inherited168PASS28.27s;
actual B DDS1PASS2.96s; moving DDS3PASS21.20s; actual controller finalzero
1PASS1.63s. Old-interface legacy fit1PASS.80s; frontend8combinations PASS;
interface build25.4s and final2package resource build4.46s PASS. An initial
binding assertion assumed a generated symlink resolved under install; its
fixture-only failure is retained and corrected binding9owners/IDL/resource PASS.

Use clean PYTHONPATH and source Humble, then Q2 local_setup, then Q5 local_setup;
append /home/mattb/dsim-lab/extremum-seeking/src. Final installed ros_esc and
launch bindings resolve held live source; generated IDL resolves the Q5 external
build. Do not prepend the source package root with stale tracked egg metadata.
No required Q5 source/transport test is unavailable. Actual DDS inputs are
synthetic supplied candidate/pose/cost evidence; they establish integration,
not detector sensitivity or real-field Gaussian behavior.

## Remaining objective

Q2v2 remains CLOSED_EVIDENCE_UNAVAILABLE with confirmation sealed; Q3/Q4 source
fixes do not change its outcome. No M4 acquisition has run. Next declare one
finite scientific development decision addressing window/orbit sensitivity,
independent settling labels, M3 evidence availability and direction evaluation
without another unbounded prerequisite chain. Preserve the agreed formula and
acceptance targets unless a separate methodological change is explicitly
adopted. Execute the approved four visible development and twelve frozen holdout
pilot/report with honest scientific outcomes and fixed integrity stop conditions.
The overall V2 goal remains IN_PROGRESS.
