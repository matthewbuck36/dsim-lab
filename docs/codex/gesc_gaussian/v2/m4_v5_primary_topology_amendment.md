# M4 v5 primary topology prerequisite

Adopted 2026-09-10 UTC before the following source correction or any new
topology derivation. This bounded scenario/evidence amendment is within the
user's full V2 goal and authority for recommended corrections. It supplements
`m4_v5_moving_fill_plan.md`; no v5 preparation/acquisition has started.

## Concrete source requirement

The unchanged schema14 owner in `scenario_schema.py` requires
`local_association_mode=verified_trap` plus an authoritative topology record
when the bounded interior-anchor fallback is enabled. The original primary
v8.10 template uses `declared_source` without that record. The secondary
already uses `verified_trap`. The first v5 routing implementation therefore
passes routing fixtures but is not yet admissible through the actual schema.
This gate must remain intact. It was found before preparation, not during a
failed simulation; no v5 numerical or empirical attempt has been consumed.

A read-only scenario-resource search found no authoritative receipt matching
the exact original primary source list/start. The v8.12 probe's local radius
1.25 m differs from the original primary 1.50 m, so its receipt cannot be
imported. Preserve both source arrangements and all old versions/results.

## Selected minimal amendment

For fresh v5 slots1–4 only, adopt `verified_trap` local association and attach
an authoritative primary nominal topology record. Generate that one record
with the **unchanged existing**
`aggregate_field_truth.derive_two_source_topology_qualification`, from the
exact primary v8.10 sources/start/bounds and nominal disturbances, inside the
same single 600-second preparation allowance. No extra acquisition or separate
topology search, optimizer tuning, grid, margin, threshold or budget renewal.
If the ordinary finite topology qualification fails, preserve that preparation
failure; do not substitute a different map or relax the gate.

The original evaluator geometry receipts and independent position labels
remain unchanged; this is an additional scenario admission proof, not a new
label geometry or certification of attraction dynamics. Primary behavioral
association now uses the schema's existing stricter verified-trap contract;
all four development arms share it. Holdout association, scientific metrics,
192 targets, denominators, success predicates, seeds and control settings
remain unchanged. Existing v1–v4 source behavior stays selectable unchanged.

Extend only the existing M4 scenario/workflow owners with an explicit v5
primary topology input and receipt. Keep secondary nominal/noise/delay records;
add a separately named `primary_nominal` receipt for v5. Require that exact
record in all four resolved primary cases and pin/check it with the existing
receipt owner. Preserve old version/default argument behavior. Update the
independent frozen audit to verify all four topology receipts and their exact
source/start/bounds/disturbance/model hashes; no independent numerical rerun.

Before preparation, test actual schema selection/delegation with finite
declared topology fixtures: missing record, declared-source substitution,
wrong primary input/receipt and cross-arm mismatch must be rejected. Numerical
derivation in source tests is mocked explicitly and is not qualification.
Check that the production path delegates to the unchanged numerical validator;
the eventual single preparation executes that validator normally. Extend old
version parity, all16 controls/argv, frozen receipt and source-pin tests.

No algorithm/node/numerical-owner/schema guard modification is authorized by
this amendment. No physical/Pi, V1, commit/push, retained-result rewrite or new
simulation is released. Source validation/checkpoint, one preparation, frozen
audit and exact release remain required under the parent plans.
