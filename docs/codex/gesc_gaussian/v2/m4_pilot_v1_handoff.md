# M4 pilot v1 handoff: closed incomplete

Read [result and postmortem](validation/m4_pilot_v1_result.md), current status,
AGENTS.md and live Git before continuing. The single fixed M4v1 dispatcher has
finished exit 1. It must not be restarted. One baseline A development slot is
INCOMPLETE because outer process-ownership proof failed; the other 15 slots
are UNSTARTED. No block analysis, holdout release or replacement occurred.

The retained inner baseline reached ranked GOAL_HOLD and passed 14 required
predicates, recording, final zero and inner strict cleanup. The outer periodic
tracker encountered a disappearing-process exception. Its final inspection
loop was skipped; empty outer arrays cannot be cited as successful cleanup.
The all-16-slot report preserves unavailable results and every one of the
192 direction targets. Neither research improvement has been established.

Q7's two-block mode is implemented and source-validated (400 integrated checks),
with the inherited PDE and five-shift modes selectable. M4 execution/evaluation
source is validated (767 unique checks in the correct overlay contexts).
All 606 frozen source hashes stayed unchanged through acquisition and closure.
The user-confirmed goals remain faster trapping detection, including circling,
and better GESC directions while moving without mandatory stopped acquisition.

External root: `/home/mattb/Experiments/GESC-Gaussian/v2/pilot/m4_pilot_v1/`.
Read `acquisition/acquisition.json`, `acquisition/slot_1.json`,
`report/report.md` and `report/result.json`; their exact hashes and the retained
baseline run directory are in the result. Closure audit and console are under
the sibling `builds/m4_pilot_runtime_v1/`. All 50 pilot files, including bag,
are hashed by `pilot_closure_audit_v1.json`.

Next incomplete task is a separately planned correction of the process
observation race, with finite source tests preserving strict cleanup proof.
Do not erase the error, relax the gate, repeat this dispatcher, overwrite the
report or merge its baseline into a new comparison. Any new acquisition needs
a new version and explicit release under a reviewed budget. The approved fixed
16-slot version is closed; V2's research objectives remain IN_PROGRESS.

Branch `feature/gesc-gaussian-robustness-v2`, HEAD `3369cfc`. Dirty/untracked task
work is saved and retained in material checkpoints; no commit or push was
authorized or made. Simulation only: physical workspaces/Pi/hardware and V1
remain untouched. The newest current-status receipt identifies the final
archive after it is created.
