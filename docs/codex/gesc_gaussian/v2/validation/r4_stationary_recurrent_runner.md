# R4 stationary recurrent runner source boundary

SOURCE_VALIDATION_PASS, 2026-09-10, under the prospective
[stationary integrated01 plan](../r4_stationary_integrated_01_plan.md).
The unchanged recurrent method can now enter the existing runner through the
named robust simulation DEVELOPMENT scenario. No Gazebo, real bag read,
direction job, matrix, numerical retuning or historical-result revision occurred
in this source boundary. The actual integrated outcome remains pending.

## Existing owners and preserved authority

`scenario_schema.py` admits recurrent/stationary only for the named schema14
method-development suite, robust singleton profile and development partition.
It adds the canonical stationary recurrent request topic to the existing
override allowlist and validates its fixed value. Centered verification still
requires rolling mode. Existing simulation-only, state-gating, source-gap,
topology-binding and arrival restrictions remain unchanged. In particular,
arrival retains the counted two-source development contract,0.5m boundary,
complete fill/recovery and ownership predicates; ranked GOAL_HOLD is optional.

`run_scenario.py` permits that named stationary route in the existing event
selector. The existing diagnostic and Timekeeper subscriptions and serialized
offline reader already select the recurrent wire. Before private event timestamp
adaptation, the stationary branch now calls the shared full persistence/history
origin check. Existing exact identity, event geometry/score, run, frame, source
and publication joins remain required. The moving and legacy coordinate
branches are unchanged.

The model history endpoint remains the private convergence coordinate; the
latest input pose and publication remain separately audited. The stationary
request's later publication remains its own fill correlation. The existing
unique preceding convergence association already accepts convergence<=fill and
needed no rewrite. Exact nested request-to-recorded-diagnostic and request/result
authority stay in the previously validated recorder/stationary validator; the
live monitor does not independently reconstruct that request ledger.

The earlier final pairing launch correction routes selected/delayed algorithm
pose to stationary GaussianFill, with actual frontend evidence recorded in
[protocol validation](r4_stationary_recurrent_protocol.md). That source was
archived before these runner edits. This subset changes only the two named
production owners, a new focused test module and the earlier test asserting the
formerly incompatible selection. The existing protocol roundtrip assertion was
also corrected to compare canonical full wire fields after real CDR roundtrip,
avoiding native padding-byte instability. No request/hash semantics changed.

## Retained tests and scope

External root:
`/home/mattb/Experiments/GESC-Gaussian/v2/development/20260910/stationary_integrated_01/`.
Context validation `timeout --signal=INT --kill-after=2s 28s
docs/DSIM_GESC_Gaussian_Codex_Implementation_Package/tools/validate_phase_context.sh
v2 implement` passed before edits. Scoped diff check passed. A read-only diff
against `checkpoints/r4_stationary_recurrent_source_v1/source.tar.gz` confirms
the small runner/schema changes; the exact patch and unchanged archived scenario
list are retained as `runner_source_changes.patch` and
`runner_unchanged_archived_scenarios.json`.

The first job, PTY45244, exited1 after22.07s wall:138passed/four failed new
fixture/assertion expectations. Failure logs and before/after pins remain in
`runner_tests_v1_{prepared.json,log,time.txt,receipt.json}`. The failures were:
assuming every frontend explicitly forwards its existing simulation-clock
default; changing pose delay without changing retained topology qualification;
exact floating equality after nanosecond-to-second conversion; and expecting
False instead of UNAVAILABLE/None for an absent typed join. The topology guard
correctly rejected its mutation. Production code was unchanged for the corrected
job; the nominal scenario remains unchanged and a topology-binding mutation is
now an explicit rejection case. Delayed pose routing was already verified by
the separate actual frontend pairing tests.

The corrected job used the current generated interface overlay and reserved
localhost-only domain217:

```bash
set -o noclobber
timeout --signal=INT --kill-after=2s 118s env -u PYTHONPATH bash -c 'source /home/mattb/Experiments/GESC-Gaussian/v2/development/20260910/stationary_recurrent_pairing_v1/environment.sh; export PYTHONPATH=/home/mattb/dsim-lab/ros2_ws/src/ros_esc:$PYTHONPATH; export ROS_DOMAIN_ID=217; export ROS_LOCALHOST_ONLY=1; exec /usr/bin/time -o /home/mattb/Experiments/GESC-Gaussian/v2/development/20260910/stationary_integrated_01/runner_tests_v2_time.txt -f "elapsed_seconds=%e exit=%x" python3 -m pytest -q ros2_ws/src/ros_esc/test/test_r4_stationary_recurrent_runner.py ros2_ws/src/ros_esc/test/test_recurrent_recording_selection.py ros2_ws/src/ros_esc/test/test_r3_arrival_evaluator.py ros2_ws/src/ros_esc/test/test_m4_centroid_event_evaluation.py ros2_ws/src/ros_esc/test/test_m4_monitor_progression.py ros2_ws/src/ros_esc/test/test_r4_stationary_recurrent_protocol.py' > /home/mattb/Experiments/GESC-Gaussian/v2/development/20260910/stationary_integrated_01/runner_tests_v2.log 2>&1
```

PTY28615 exited0/reaped:181passed in23.49s pytest/23.92s wall. All14 source,
scenario and plan pins stayed unchanged, including the frozen core and full
recurrent checker. Preparation, log, wall time and terminal receipt remain under
the `runner_tests_v2_` prefix. Both test jobs consumed45.99s measured wall total;
each had its own118+2s outer cap. The failed first job is not promoted or erased.

The new selected tests exercise actual schema expansion, launch argv and metadata,
full frontend consumer parameters, incompatible scope/topic/clock/policy and
arrival gates, original publication identity, preorigin persistence rejection,
live Stage A followed by arrival without ranking, conflicting/missing evidence,
and the existing offline owner using real serialized messages with an in-memory
reader fixture. The valid stationary wire fixture has nonzero Timekeeper origin,
history endpoint60s, latest pose60.009s, publication60.1s, and later valid request
and fill correlation70s. Missing typed evidence stays unavailable and cannot
authorize arrival. Legacy/moving timestamp joins and monitor timing regressions
also pass. These tests establish source integration, not an empirical Arm B result.

Production source is held after this boundary for the parent's single-case
freeze. Root owns acquisition, recording completeness, analysis release and
navigation/checkpoint updates. The full four-arm comparison remains unreleased.
