# M4 execution and evaluation source validation

Status: CLOSED_SOURCE_VALIDATION_PASS, before any M4 preparation calculation
or acquisition. Final606 source hashes are stable across all validation jobs.
Authority: the approved V2 comparison, user continuation request, and adopted
`../m4_execution_evaluation_plan.md` with its bounded source clarifications.
Q7 remains a separate closed source milestone; none of these source checks
establishes the two research performance targets.

All logs, command files, source snapshots and receipts below are retained under
`/home/mattb/Experiments/GESC-Gaussian/v2/builds/m4_pilot_runtime_v1/`.
Counts overlap; do not add successive or combined selections together.

## Implemented boundaries

- The existing scenario/runner owners expand exactly sixteen matched A/B/C/D
  slots, preserve inherited controls, select B/D two-block parameters and the
  fixed C/D policy, and require exact disturbance-bound topology evidence.
- Strict selected cleanup records observed PID/start-time/session ownership,
  treats inspection failure explicitly, and preserves legacy defaults. The thin
  dispatcher uses M4A deadlines and canonical existing one-case runner commands.
  Actual launch, recorder metadata and captured noisy JSON have independent
  equality/hash checks; only the declared noisy temporary path may relocate.
- The existing analyzer now has an explicit M4 augmented-objective adapter.
  Recorded Gaussian/affine laws and composition timestamps feed the existing
  numerical reference owner. Q1/Q2 keep their old empty-law restrictions.
- `m4_pilot.py` owns finite independent labels, first-opportunity censoring,
  actual publication-decision latency, fixed direction targets and aggregation.
  Missing endpoints, errors, stop attribution and direction analysis stay
  unavailable. Every one of192 scheduled direction rows remains inspectable.
- The existing Stage A evaluator/live monitor joins selected typed centroid
  confirmations to private normalized legacy event records. Public messages,
  existing clocks, source support timestamps and historical meanings are held.
- Thin docs tools stage one filtered bag read per run for science, frozen labels
  before event joins, finite references, summaries, the one-time holdout release
  and a local report. Acquired identities and behavioral failures cannot be
  substituted by scientific result rows. Source/configuration/receipt changes
  or failed integrity abort dispatch; clean scientific timeouts stay outcomes.

## Retained focused evidence

- Metrics: `metrics_source_receipt_v1.json`, SHA256
  `5fb8ebc1f74bf249c63fe0f4e851a70c0c2f7c406fd0fa289f1bad642efad4c7`.
  The initial21000-pose fixture took116.09s and failed its finite requirement.
  Identical right-sided timestamp lookup semantics using `bisect_right` reduced
  it to6.10s.159 focused/inherited checks, then21 metrics checks and4 final
  aggregation checks passed at the recorded source versions. Later additive
  row/availability changes are covered by the final combined receipt.
- Objective adapter: `objective_inputs_receipt.md`;169 checks passed in23.18s
  including45 new and124 inherited cases at that source version.
- Typed event compatibility: `centroid_events_receipt.md`; final38 checks passed
  in1.11s with stable source/test hashes. Five earlier synthetic failures used
  an inconsistent doubled-window sample count; fixtures alone were corrected.
  Generated CDR and fake bag/live callback tests retain the original wire bytes.
- Dispatch/cleanup/science: `dispatch_receipt_v1.json` and
  `dispatch_science_receipt_v2.json`;79 focused/inherited checks, then47 combined
  dispatcher/science checks in5.06s. Later exact launch-witness checks are
  retained separately and included in the final integrated selection.
- Workflow: `workflow_receipt.md`, SHA256
  `501bf2fda5076346dadd5a0fa1431486097a039ec907182ce4966a171d0c3822`;
  final44 checks passed in2.11s with stable source/test hashes. Includes actual
  sixteen-case schema expansion with only numerical owners mocked, immutable
  release/identity substitution faults, clean timeout/abort paths, and exact
  sixteen acquisition statuses plus192 target rows.
- Staged evaluator: `evaluation_cli_v*.log` retains its incremental focused
  checks. No actual recorded inputs or new numerical fields were evaluated by
  these fixtures. Final receipt will bind the held combined source below.

The speculative topology cache was removed before preparation because its
owner-file edit conflicted with the original geometry byte receipt. Its source,
tests and passing logs remain in `removed_cache_source_v1/`. The numerical owner
is restored exactly; ordinary uncached validation remains inside fixed budgets.

## Integrated release evidence

`root_final_validation_v1.py` used a360s test cap inside a420s wrapper,
the existing Q2/Q5 isolated overlays and ROS domain77. It retained745 PASS,
3 FAIL and19 ERROR, with1 extra Gazebo test deselected, in88.68s. Every failure
was in the two historical installation-specific test modules: their explicit
initial/Q2 ros_esc prefix expectations correctly rejected the selected Q5
package. Source hashes were stable; no algorithm/source/test change followed.

The two complete historical modules then passed in their declared ros_esc
package contexts: Q1 initial23 PASS2.32s and Q2 19 PASS2.34s. Their package.bash
scripts selected the expected wrappers/metadata while current Q5 generated
interface dependencies remained available. No rebuild or ROS startup occurred.
Exact shell commands,85s test/90s wrapper caps and606 before/after source hashes
are in `root_environment_selected_commands_v1.txt` and the two
`root_environment_q{1,2}_v1_receipt.json` files. All source maps equal the
original integrated invocation. Twenty of these42 cases already passed there;
combined coverage is **767 unique passing test identities**, not809.

The current Q5 installation separately resolves all21 console targets and both
actual `record_run --help` and `run_scenario --help` wrappers pass under20s caps
without startup; see `m4_actual_entrypoints_v1_receipt.json`.

Final composed receipt: `root_final_composed_v2_receipt.json`, SHA256
`8bc1014a01241c1364d966f19a462c5bbf9576c99b55577faa2d17429f360207`.
It binds all four constituent receipts, exact22 initially rejected identities,
767 unique passing tests, two additional actual CLI checks and606 held source
hashes. The original mixed-context invocation remains failed evidence.
The inherited optional extra headless Gazebo E2E was excluded: the next
simulation belongs to the approved development population.

Material release: review the held source diff, validate context, run the existing
V2 checkpoint tool, archive the complete dirty task tree and retained receipts,
then attempt the single600s preparation job. Public dispatch additionally
requires a prepared contract, passing immutable source-validation receipt and
verified material source archive. Holdouts remain sealed through development.

No physical snapshot, Pi, hardware, V1 branch, commit or push is involved.
