# M4v11 workflow and runner validation

SOURCE_VALIDATED; coordinated root bundle passed as recorded below.

The adopted [V11 plan](../m4_v11_draft_plan.md) registers the exact arrival family
in the existing runner and workflow. Preparation and verification bind the
selected runtime, corrected source/evidence and external orchestration helpers.
All four nested scientific document checks use the frozen contract version.
V11 release additionally recomputes both nominal first-opportunity latency pairs
from retained rows, compares the exact summary and requires both observable.
This preserves complete censored science, V10 release behavior and the unchanged
final30% estimand. Finalization retains all16 slots and an explicit unavailable
development-pair reason. Strict cleanup, shared-daemon admission and late nested
receipt checks remain authoritative.

Owned source: `tools/m4_workflow.py`, existing `run_scenario.py`; new fixtures
`test_m4_v11_workflow.py` reuse the V10 preparation/science receipt fixture and
the actual position-label/first-opportunity helper from V11 science tests. Two
legacy route tests move only the unsupported sentinel from V11 to V12.
Static AST, scoped whitespace and exact required-path checks passed. No tests,
production imports, bags, models, preparation or runtime were executed here.

Before copies, owner patches and held hashes are retained at
`/home/mattb/Experiments/GESC-Gaussian/v2/development/20260910/m4_v11_source_v1/`,
including `workflow_source_ready.json`. Root owns the single coordinated finite
source bundle and will retain its exact result before any preparation.

Root completed the sole source bundle:676 unique checks PASS160.293120s,
806 stable pins/21 installed entries,session4096 terminal0. Full command and
receipt are in [V11 source validation](m4_v11_source.md). No preparation or
acquisition was performed as part of that validation.
