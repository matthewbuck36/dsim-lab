# M4 v3 recorder-shutdown validation

Status: CLOSED_SOURCE_VALIDATION_PASS,2026-09-10 UTC. Active amendment:
[m4_v3_recorder_shutdown_plan.md](../m4_v3_recorder_shutdown_plan.md).
This is source/process validation before any fresh preparation/acquisition.

## Preserved failure and scope

M4v2 is immutable CLOSED_INCOMPLETE. Closure manifest:
`/home/mattb/Experiments/GESC-Gaussian/v2/checkpoints/m4_v2_closed_incomplete_v1/manifest.json`,
SHA256 `846f535e46e86389fd632cbb1dcf094d9d640f9dc2596ded374b65bd2765f492`.
The report retains one incompleteA,15unstarted, all192direction rows and no
scientific acceptance. Inner/outer kernel proofs and performed cleanup PASS;
recording/final zero are unavailable because recorder shutdown was delivered
only during escalation. Source remained610/610 held through closure, every46
pilot files including401125376-byte raw DB were hashed without modification.

The prior goal turn advanced source/preparation/release. Current continuation
verified the same live dispatcher until terminal exit1, audited/closed the
failed result, and recorded the new amendment before new edits. This is
progress, not a restart prompted by an observation timeout.

The new `subreaper_group_v3` selects one identity-guarded initial group SIGINT
inside the existing owner, with later pidfd cleanup and original absolute
ends. Inherited/default observed-tree and selectable subreaper_v2 stay intact.
The fresh experiment version is `m4-pilot-v3`; scientific method remainsv1.
No detector/GESC setting, numerical owner, scientific target, denominator,
recording/cleanup gate or original case/suite/science budget is relaxed.

## Installed wrapper and finite process requirements

Actual `/opt/ros/humble/lib/python3.10/site-packages/ros2run/api/__init__.py`
lines64-71 launches its executable with Popen, then catches KeyboardInterrupt
without forwarding PID-only SIGINT. SHA256
`bdc67613b3e94a149f184f5a9adb03f08f6894a7f045ed02f0af5bef75d06c47`;
read-only copy and receipt are external `installed_ros2run_api.py` and
`installed_ros2run_receipt.json`. This installed wrapper byte receipt is also
required in frozen source before acquisition. Earlier direct-Python process
fixtures did not cover that real wrapper hierarchy.

New fixtures must use this actual installed wrapper, a finite recorder handling
SIGINT/SIGTERM, and target/bag children in separate sessions. The recorder's
ordered target stop, bag flush, final-zero marker and metadata finalization
must take more than the scaled escalation allowance but fit the original
graceful period. Preserve the PID-only failure and verify that new-mode first
SIGINT reaches the recorder without prematurely signaling those child sessions.
Signals and guard failures require accurate phase/time/group provenance; actual
recording/final-zero acceptance still comes from runtime evidence, not a marker
fixture or successful killpg. Kernel and legacy regression checks remain required.

## External orchestration artifacts and pending results

Evidence root: `/home/mattb/Experiments/GESC-Gaussian/v2/builds/m4_v3_recorder_shutdown_v1/`.
Before-source copies belong to `ownership_before_v1/` and `version_before/` with
manifests. Focused test commands/logs/receipts will be added below after execution.
`root_source_validation.py`, `actual_entrypoints_v1.py`, `archive_boundary.py`,
`run_preparation_v1.py`, `frozen_audit_v1.py` and `release_dispatch_v1.py` are
prospective copied/adapted helpers, not completed results. They are external
orchestration around existing owners, not replacement runtime/science pipelines.

Planned final environment is clean Q2 then Q5 overlays, ROS_DOMAIN_ID183,
ROS_LOCALHOST_ONLY1, DISPLAY0. No fresh root/acquisition exists at this update.
Final source validation must precede the one600s preparation/audit/release.
The canonical finite dispatcher remains15300s maximum, with four visible
matched development cases before one-time frozen twelve-holdout release.

No commit/push, physical/Pi, V1 or unrelated workspace action occurred.

## Retained baseline and preservation checks

The exact missing hierarchy reproduced PID-only failure once, against the
retained v2 runner SHA256
`8f3b8b3d0db87a6c109b14250a99acb0e4da8bd12081f473630a6507b1b73355`
and installed wrapper above. The deliberate baseline assertion failed3.47s;
wrapper4.024929s, before/after pins stable. Recorder received SIGTERM first,
wrote its final-zero marker, then was killed before ordered target/bag shutdown
and final metadata. This marker is a fixture event, not physical/runtime proof.
`baseline_events_v1.json`, `baseline_command_v1.json`, `baseline_v1.log` and
`baseline_receipt_v1.json` retain exact evidence. Receipt SHA256
`d076abfb5d9591a07cd0198b7d310ad2827d5ec03f32539fce5af48c2316561d`.
The corrected-mode fixture retains the same timing; results are pending.

Read-only old-evidence verification passes all96 files across M4v1/v2 plus both
closure archives,1.421803s. `retained_evidence_check_v1.json`, SHA256
`3cf0b3ec1ff17a43a17ad7de5239be36de5c8e533e636ff1379e7b7e3dc602f5`.
Live source is under authorized v3 edits; historical source remains protected
by the immutable archives. No raw bag reindex or science rerun occurred.

A historical route test classified `m4_pilot_v3` as an unsupported future suite.
Since this amendment now approves exactv3, its negative fixture may advance to
still-unsupportedv4 after preserving original bytes/hash. The unknown-version
rejection remains unchanged; v1/v2 positives and unrelated/empty negatives stay.
This specification-driven fixture update must be recorded with final validation.

## Focused source and process outcomes

Version propagation passes155 focused checks12.01s with held source. Exact
receipt `versions_source_receipt_v1.json`, SHA256
`bf8e7af42399de37328b6b7a36648d6849a61114158425ef6d373004353fb069`.
New exactv3 root/IDs/mode/strategy/192targets and oldv1/v2 parity/rejection are
covered. Only m4_scenario/m4_workflow/run_m4 and the new version test changed;
metric/evaluator numerical sources are unchanged. First run154PASS/1 new-test
suite-ID expectation typo is retained; correction changed only that new fixture.
Pre-edit manifest SHA256
`3c912f704a48a69a31b1ebe790f95bbf893fda1c7ab3c87cb30500dbb73f08be`.

Independent actual-wrapper/shutdown suite passes12 checks16.83s, stable
runner/test/installed-wrapper pins. Consolidated receipt:
`independent_shutdown_validation_v1.json`, SHA256
`6833a8e0c32978ba30b9350b70c493b025e30824ed0dcd4adbea7a9faf846d14`,
binds19 command/log/event/pin receipts, including the retained old-route failure.
Final actual-wrapper fixture returns0 in1.953901s: first signal is SIGINT,
final-zero marker then target stop then bag flush then finalized metadata;
resource signals are nonpremature, one guarded group interrupt, no TERM/KILL,
and all eight kernel flags pass. Identity substitutions, permission rejection,
reaped/adopted root, foreign wait consumption, native admission, interruption/
forced escalation/original deadline and real root exit after the guard pass.
These markers prove the declared fixture behavior, not empirical bag readiness.

The first combined owner-route invocation reports108PASS/1 new-test loader
fixture omission. Existing suite admission correctly rejected its nonexistent
fixture path; source stayed held. The failure and pre-fix new test are retained.
Only the fixture loader is corrected, with a focused recheck followed by the
required final integrated suite. Earlier overlapping totals must not be added.
Final integrated/installed CLI/preparation/acquisition results remain pending.

## Final integrated source boundary

Final **872 unique tests PASS**, one predeclared Gazebo E2E deselected,
pytest146.48s / wrapper147.294272s. All615 source/configuration pins are
identical before/after, including the actual installed ros2run wrapper.
`root_final_v1_receipt.json`, SHA256 `c27a49b0489d7c6005c1ec8337e380ba046730bacc7dcfbb778bdb58794331d3`,
retains exact environment/command, every JUnit identity and21 installed Q5
console bindings. Raw log and JUnit XML are in the same external directory.
Focused counts overlap and are not added to872.

The owner source hold receipt `ownership_source_hold_v1.json`, SHA256
`a886f6dd28d9697482b1aa9f60a1b0f68c6fff7500ad6222eda01910dd56945f`, binds
108 initial passes and the single corrected loader fixture PASS0.61s, stable
production and retained original failure. The integrated result above verifies
all final tests together. No source correction was needed after those fixture
fixes; no test was skipped to obtain the result.

Actual installed runner/recorder `--help` checks PASS under20s each. Runner
advertises exact `subreaper_group_v3`; `actual_entrypoints_v1.json`, SHA256
`31d793e4f278042f9c970af30b6e708aee9352a7210392ab404ff41378cf6f83`.
These checks verify parser/installed selection, not runtime readiness.

Exact integrated command:

```bash
env -u PYTHONPATH ROS_DOMAIN_ID=183 ROS_LOCALHOST_ONLY=1 bash --noprofile --norc -c '
source /opt/ros/humble/setup.bash
source /home/mattb/Experiments/GESC-Gaussian/v2/builds/q2_policy_runtime_v1/install/local_setup.bash
source /home/mattb/Experiments/GESC-Gaussian/v2/builds/q5_stationary_centroid_adapter_v1/install/local_setup.bash
export PYTHONPATH="$PYTHONPATH:/home/mattb/dsim-lab/extremum-seeking/src"
timeout --signal=SIGINT --kill-after=10s 510s /usr/bin/python3 /home/mattb/Experiments/GESC-Gaussian/v2/builds/m4_v3_recorder_shutdown_v1/root_source_validation.py root_final_v1'
```

The wrapper retains exact inner480s pytest argv,10s kill grace,495s subprocess
bound. It selects all14 `test_m4*.py` files plus11 existing runner/study/Q7/
analysis files. Historical Q1/Q2 installation-specific checks retain their
previous correct environments; no wrong-overlay rerun or rebuild was needed.
CLI helper ran under55s in the same environment using `actual_entrypoints_v1.py`.

Independent production review finds old observed/plain execution, specialized
live monitors, launch commands and scientific event helpers source-identical.
New-mode critical section uses nonconsuming wait then identity/time guard and
group interrupt; separate-session resources remain owned by the recorder.
Receipts distinguish group/pidfd delivery, failure/skip, phase and monotonic
stamp. Existing kernel completion, pidfd escalation and absolute budgets stay.
Science changes only select mode/provenance; worker/numerics are unchanged.
Against the prior610 pins, 604 files remain identical,
6 planned source/test files change and 5 pins are added.
The sole old-test change advances an unsupported future suite fromv3 tov4
under this approved amendment; its original bytes and receipt remain retained.

This closes source/process validation only. Real recorder finalization under
the selected Gazebo route and both original research targets remain unproved.
Next: material source archive, one600s preparation, independent exact frozen
contract audit/release, then the finite four-development/twelve-holdout pilot.

## Source archive

M4v3 material source archive saved: external
 `checkpoints/m4_v3_source_closed_v1/manifest.json`, SHA256
 `d4915b01763251bd40d01bab9006568f68d8847f3e76646fcc8192698dfe4d79`;
 347 dirty/untracked files,62 artifact references,1372937-byte verified tar.
 Context/diff/links/checkpoint PASS;615 source pins held. This live receipt
 postdates the immutable archive. Single600s preparation is next; read external
 `builds/m4_v3_recorder_shutdown_v1/preparation_v1*` and fresh pilot preflight
 before recovery. No acquisition is released yet.


Exact archive command: `timeout 60s python3` on external
`builds/m4_v3_recorder_shutdown_v1/archive_boundary.py m4_v3_source_closed_v1`.
Existing V2 context/checkpoint tools ran under30s each; `git diff --check` and
615-pin/local-link assertions passed. No additional source changes occurred.

## Preparation and frozen audit

M4 v3 preparation and frozen audit PASS, 2026-09-10 UTC.
The single preparation finished in43.008776s within its600s maximum.
Contract `pilot/m4_pilot_v3/preflight/contract.json` SHA256:
`f035a5c7de675d505f10952d06e55ad77166ee235512f8e10f5069cbf92918fb`.
Prepared receipt SHA256:
`27afb2847c0ab5a7a96bd6fe4e32b892feb75326851675f8003d13a76d74aa46`.
Independent frozen audit PASS2.953708s, no field reevaluation or bag decoding.
It verifies615 source pins, the installed wrapper against the retained baseline,
all16 exact slots/commands, selectors, controls, geometry/topology/configuration,
noise/delay receipts and every execution/science cap. Audit receipt SHA256:
`5f6c86e1c26e271e4bde843cd1d09011f0e1de694fe21fa1306d48bb7cf87295`.
Source is held; no acquisition or holdout release has started.
Next: material preparation archive and exact dispatcher release.

Exact environment is the final integrated command above (domain183/local-only,
clean Q2 then Q5 overlays). Preparation outer command:
`timeout --signal=SIGINT --kill-after=5s 595s /usr/bin/python3`
on external `builds/m4_v3_recorder_shutdown_v1/run_preparation_v1.py`, giving
600s maximum including escalation. The retained receipt records inner590s
work/5s kill command and actual elapsed time. Audit:
`timeout 30s /usr/bin/python3` on external `frozen_audit_v1.py` in that directory.
No retry or evidence replacement occurred.

## Exact acquisition release

M4v3 DISPATCH RELEASED; its single acquisition is starting. Release
 `pilot/m4_pilot_v3/preflight/dispatch_release.json`, SHA256
 `e6c9fbd007783814c4ba25af078116a36f7d9fb93769de4aab3adf71b43785c0`,
 binds exact contract, source validation, actual CLI, frozen audit, archives
 and initial slots1-4. Preparation archive SHA256
 `76b770e35e244e07c0c1939228d95ca314a700d9a51bad4c06bd4b8a1bdec0e1`;
 347 files,76 artifacts,1374180-byte verified tar. Canonical dispatcher command
 uses SIGINT15180s/kill-after120s,15300s maximum. Source stays held.
 Console: `builds/m4_v3_recorder_shutdown_v1/acquisition_dispatch_console_v1.log`.
 Read fresh acquisition started/slot/final receipts before recovery. Never
 restart this dispatcher or replace a slot; holdout release remains separate.


Independent read-only review of the three preparation/audit/release scripts
found no stale version/root/mode/domain/budget reference. Exact release helper
ran under30s in domain183 and verifies passing audit/current environment/
source/checkpoint bytes before calling the public release validator. Release
is for initial four slots; holdout release requires the separate runtime gate.

## First actual simulation validation

M4v3 slot1 A COMPLETE/integrity and behavior PASS in413.875127s.
 Actual recorder finalizes target/bag exit0 and final zero; completeness48/48,
 behavioral predicates14/14. Inner/outer kernels and performed inner/outer/
 independent-inner cleanup all PASS. Exactly one guarded groupSIGINT and no
 TERM/KILL escalation. Slot receipt SHA256
 `c9b10118da40ae9428125b72a315b576a2fafb63f8274225e5270c3909372b6e`.
 Read-only first-slot audit SHA256 `0832d70451d6aa6f6581a4901483e6fe730b46f047c3d5c046f10fddef98ba6b`,
 external `builds/m4_v3_recorder_shutdown_v1/first_slot_audit_v1.json`, binds
 completed input hashes and615 unchanged source pins. No science rerun or
 active-slot bag inspection. ArmB slot2 is recording; same dispatcher remains
 active. No M4 block science/research acceptance yet; source stays held.


This demonstrates recorder shutdown/completeness and ownership in one actual
baseline simulation. It does not demonstrate the new detector or moving-search
performance. The canonical dispatcher retains immutable first-slot input hashes
while later slots run; no archive hashes a changing active bag at this boundary.
# Development slot2B: complete behavioral failure

The same single dispatcher admitted slot2B COMPLETE with integrityPASS after
491.340042s. Selected two-block/stationary launch and captured parameters match
the frozen contract. The existing finalized validator reports10702 typed
diagnostics and zero confirmations/stationary requests/fills. SEARCH reached
the fixed StageA timeout360.026sim seconds; behavioral predicates pass4/14.
Recording completeness52/52, finalzero, recorder/target/bag exit0, both eight
kernel flags and performed inner/outer/independent-inner cleanup all PASS.
One guarded groupSIGINT, no TERM/KILL. Preserve this failure as an outcome;
it does not itself qualify primary latency or justify post-freeze retuning.

Slot receipt SHA256
`b0ce431669ccebdd3e7d5be007f2b2e05609a1c3a093dda35b74f98dbfb9dbb3`.
Root read-only audit under `timeout 30s python3` rehashed all8 finalized slot2
inputs and checked receipt/kernel/cleanup/completeness assertions in1.110308s.
External `builds/m4_v3_recorder_shutdown_v1/second_slot_audit_v1.json`, SHA256
`6b9df013f63cb3bdaad1472455897073805b9bd51fa83d28985a823402cc2e07`,
retains the exact compared input hashes and outcomes. No active slot3 bag was
read, hashed or analyzed; no runtime intervention/source change occurred.
