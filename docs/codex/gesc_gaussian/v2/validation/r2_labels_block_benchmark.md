# R2 four-arm labels benchmark result

TIMEOUT, 2026-09-10. The D3 correction does not make this full development block
fit the original120-second inclusive allowance. Preserve the attempt; no
unchanged retry, extra allowance or historical V9 promotion is made.
Prospective route: [benchmark plan](../r2_labels_block_benchmark_plan.md).

Evidence root:
`/home/mattb/Experiments/GESC-Gaussian/v2/development/20260910/labels_block_benchmark_v1/`.
Preparation completed in1.724 seconds, retaining97 current pipeline/generated
binding sources,34 small historical inputs, four declared bags totaling
1,355,608,064 bytes and a scoped source/evidence archive. `prepared.json` SHA256
`89de7fc81b953cfb4bab6a17dd58c13b5c340bce8970115fc1d0bf30d449326b`;
archive SHA256 `293a6e944507daefa86a2e9b0be8240c378808abe324220380f4e88199a79d3d`.
No retained bag was read during preparation.

The unchanged finite science owner used the original120-second inclusive cap,
including its3-second cleanup reserve. It interrupted useful work at117 seconds
and completed in117.552 seconds. `job.json` records timeout, integrity passed,
clean termination, terminal/reaped child and no remaining owned processes.
Its SHA256 is `7a5842eea728fbe56790d46044f6509d59af672c5efe2121d275b7e0cc650125`.
All scoped source and small input hashes plus all bag filesystem identities
remained unchanged. The original acquisition owner verified all four bag SHA
receipts inside the work allowance. Each run entered the existing bag reader
once; D's decoding was interrupted. No reference/model evaluation ran.

| Arm | Selected records decoded | Read seconds | Per-run analysis seconds |
| --- | ---: | ---: | ---: |
| A |177442|11.269|5.560|
| B |187264|22.463|5.056|
| C |178667|16.156|42.594|
| D |incomplete|started at113.586|unstarted|

A/B/C label and metrics files plus C's89,986,365-byte normalized file are
retained. D has no completed analysis and final `labels.json` is absent.
The exact commands are `preparation_command.sh` and `dispatch_command.sh`;
`progress.jsonl` records sparse stage timings, `worker.log` retains the
interrupted reader traceback, and `result.json` inventories every output SHA.
The normal code path still performs its original hashes, reads, analysis and
artifact formatting; instrumentation only records boundaries and counts.

The measured remaining work includes substantial decoding and C's42.6-second
analysis. These timings do not isolate the internal analysis bottleneck and do
not justify changing selectors, formatting, caches or numerical checks without
a separately saved correction and compatibility evidence. Full development
analysis feasibility and later comparison release remain open.
