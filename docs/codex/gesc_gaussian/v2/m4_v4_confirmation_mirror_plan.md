# M4 v4 canonical confirmation publication amendment

Status: ADOPTED FOR IMPLEMENTATION,2026-09-10UTC, under the active full V2
goal and user's authority to perform recommended bounded corrections without
further questions. Simulation only. M4v3 is closed and immutable; no retry or
replacement within that version. Both original research goals remain open.

## Evidence and bounded correction

Read `m4_pilot_v3_handoff.md` and `validation/m4_pilot_v3_result.md`.
M4v3's A/B recordings are complete (behaviorPASS/FAIL). C failed only lifecycle
mirror consistency: four typed legacy confirmations lack a matching canonical
status snapshot. Finalzero, recorder/target/bag and performed ownership/cleanup
passed. No block science or holdout release,13slots unstarted. Closure audit
binds615 unchanged source pins and73pilotfiles/942803553bytes. Closure archive
`checkpoints/m4_v3_closed_incomplete_v1/manifest.json` SHA256
`1553acb3257e96c612193c94d8460b37fbbd239384638020e59b0557cc7c9895`
preserves349 dirty/untracked files and141 retained artifact references.

The actual detector `buffer_cb` publishes canonical status before updating
qualified dwell (remaining count1); `_publish_confirmation` and V2 binding then
use the post-decision count0. The strict eight-field mirror equality is correct.
Fix publication ordering in this existing producer for selected moving/PDE
behavior so one canonical status row carries exactly the decision snapshot
published to the event and typed legacy confirmation. Preserve source/ROS time,
identity, support/freshness, numeric sign/units and default stationary behavior.
Do not emit conflicting duplicate status rows at a source timestamp or suppress
ordinary non-confirming status. Preserve all selected confirmation-policy paths.

Do not loosen the validator, ignore the count field, accept arbitrary near-time
mirrors, change detector thresholds/dwell or treat missing snapshots as valid
fills. Moving supervision already consumes typed confirmation independently;
its missing candidates/fills remain a separate behavioral outcome subject to
the unchanged neighborhood/comparability/information and12s abandonment gates.
This correction establishes publication consistency, not scientific success.

## Validation and preservation

1. Before source edits retain new exact source/test baseline copies and reproduce
   the mismatch once using the actual buffer callback through a qualified-dwell
   crossing. Existing fabricated-equal fixtures missed this integration boundary.
2. Exercise actual canonical/event/typed outputs through serialization and the
   existing lifecycle validator. Check exact equality, one status per callback,
   non-confirming output, repeated epochs and other selected legacy policies.
   Preserve explicit rejection of absent/altered mirrors and allow valid
   confirmations with no candidate snapshot as unavailable moving evidence.
3. Run focused legacy/stationary, detector/binding/lifecycle/transport and existing
   M4 ownership/version regressions with frozen before/after source hashes and
   actual installed CLI bindings. No extra Gazebo acquisition for qualification.
4. Review diff, document outcomes/skips/commands, checkpoint/archive before the
   single preparation, independent frozen audit and exact fresh dispatch release.
   All older evidence stays immutable; no original bag repair or reanalysis.

## Fresh version and unchanged comparison

Add exact allowlisted `m4-pilot-v4`, exclusive external root
`/home/mattb/Experiments/GESC-Gaussian/v2/pilot/m4_pilot_v4/`, case IDs `m4_v4_...`
and run IDs `m4-pilot-v4-slotNN-ARM-SEED` through existing identity owners.
Keep `method_version=m4-pilot-v1` and `process_ownership_mode=subreaper_group_v3`
with unchanged guarded-groupSIGINT/pidfd/deadline/kernel/cleanup semantics.
Preserve v1/v2/v3 output/selection behavior. Historical future-v4 negative tests
may advance only their unknown identifier to v5 after exact copies are retained;
new tests cover v4 and wrong-version/root/foreign-input rejection explicitly.

Keep all16 matched A/B/C/D cases, four visible primary development then twelve
secondary nominal/noise/delay holdouts, seeds26090801/2/3/4. B/D retain Q7
two-block W6s/.18m/.50m; C/D retain Q2 coherence policy with.75 mean weighting
and all M3 safeguards. No control/geometry/clock/command-limit/label/reference/
latency/direction/stop/combined-sequence change. No old-run import/replacement.

One exclusive600s preparation,720s recording/900s inclusive cases,15300s suite
including900s science. Preserve independent12s residence labels,12 endpoints/
six primary pairs, all192 fixed direction targets, and all error/availability
denominators. Four complete safe development acquisitions and attempted science
with complete outcome ledger precede one-time holdout release. Scientific failure
is retained; integrity failure stops. No post-freeze retuning/replacements.

External source/test evidence root:
`/home/mattb/Experiments/GESC-Gaussian/v2/builds/m4_v4_confirmation_mirror_v1/`.
No parallel node/recorder/evaluator, physical/Pi, V1, commit or push action.
