# M4 v2 recovery source handoff

CLOSED_SOURCE_VALIDATION_PASS, 2026-09-10 UTC. Full V2 remains IN_PROGRESS.
Both original goals remain earlier convergence detection including circling/
oscillation and better GESC direction estimates while moving. Source checks
establish implementation contracts; the16-run comparison must establish or
honestly reject performance targets.

Read `m4_v2_recovery_plan.md`, `m4_execution_evaluation_plan.md`,
`m4_implementation_clarifications.md`, `validation/m4_v2_recovery.md`, current
status and actual Git/evidence state. Branch is
`feature/gesc-gaussian-robustness-v2`, HEAD `3369cfc`; task edits remain saved
but uncommitted. No commit/push, physical/Pi or V1 action occurred.

Final external `builds/m4_v2_recovery_v1/root_final_v1_receipt.json` binds794
unique passing tests,610 identical before/after pins and21 installed Q5 console
targets. Actual installed runner/recorder help checks pass separately. Exact
commands, retained initial failures, hashes and limitations are in validation.

The existing runner/science owners now offer explicit `subreaper_v2` with
kernel ancestry exhaustion and original inclusive deadlines. Legacy
`observed_tree_v1` remains selectable/default. Fresh M4v2 selects the new mode
in outer, inner and science paths; no new control/recording owner exists.
Experiment version/root/IDs are isolated while the unchanged numerical method
remains `m4-pilot-v1`. B/D keep Q7 two-block W6s/.18m/.50m; C/D keep Q2/M3.

Next incomplete boundary is source checkpoint/archive, then **one** preparation:
`tools/m4_workflow.py prepare --version m4-pilot-v2` under the external600s
wrapper `builds/m4_v2_recovery_v1/run_preparation_v1.py`. Fresh root is
`pilot/m4_pilot_v2`. An existing root/log/receipt is evidence of an attempt,
not permission to repeat it. Use clean Q2 then Q5 overlays, ROS_DOMAIN_ID182,
ROS_LOCALHOST_ONLY1, DISPLAY0 and the existing visible development settings.

After preparation, inspect independent `frozen_audit_v1.json` and create the
exact initial `dispatch_release.json` before the single canonical dispatcher.
Preserve four visible development slots, twelve frozen holdouts,720s recording,
900s inclusive case and15300s suite including900s science caps. Failed integrity
stops a version; complete behavioral failure remains an outcome. No replacement,
post-freeze tuning or extra qualification acquisition is allowed.

M4 v1 stays CLOSED_INCOMPLETE: one attempted baseline,15 unstarted, retained
all-slot/192-target report and immutable bag/receipts. Neither v1 nor source
validation establishes the two performance goals. Preserve its closure archive
and all Q7/M4A/older evidence; read the new root's later receipts before acting.
