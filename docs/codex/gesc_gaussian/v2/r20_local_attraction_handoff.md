# R20 local-attraction study: measurements complete, candidate rejected

Status: COMPLETE, independently reviewed. The new optional field-fit, attraction-decision and continuous
probe components are implemented and all208 focused/regression cases pass.
Scientific controls and selected-field jobs are terminal/reaped0; independent
cached reviews pass. Material checkpoint receipt follows below. The full
research goal remains incomplete.

The 28-second probe tracks continuously in three declared kinematic cases with
maximum error2.51cm and bounded commands. Exact-model controls reject all512
negative fields, but admit only50/128 stronger attracting fields, below116.
Accepted positive zero locations are accurate (median2.08mm/P904.19mm), conditional
on admission. Calibration and all failed criteria remain frozen.

The actual two-light component finishes all8 retained candidates,48 fits and136
independent truth locations (203171 model calls). Both noiseless and noisy
attraction admission are0/8. The four D candidate centers have noncontractive
stationary raw-GESC Jacobians under the sufficient R20 condition and nonzero Q;
C candidates show substantial affine-model mismatch over the observed path.
This finding limits the method itself, beyond noise conservatism. It does not
prove global field structure or closed-loop unicycle behavior.

See [prospective plan](r20_local_attraction_plan.md),
[generator preparation](r20_preparation_appendix.md), and
[validation](validation/r20_local_attraction.md). External exact command receipts,
all inputs/partials, source pins, model/reference receipts and results are under:
`/home/mattb/Experiments/GESC-Gaussian/v2/development/20260911/r20_local_attraction_v1`.
Selected-field preparation is `component_preparation_appendix.md` in that root.
No old candidate identity, result, cost sign, raw-ranking owner or runtime mode
changed. New source helpers remain unwired; old runtime timing is preserved.

Next incomplete criterion: prospectively choose verification consistent with
persistent observed trapping, rather than assuming recurrence implies a local
attractor of the stationary-GESC field. Full augmented SEARCH confidence remains
separately incomplete. Do not simply relax this study's thresholds. A method
revision must be evaluated on independent controls and in a credible visible
integrated development case before any new matrix. R20 is not such a case.

Arrival within the evaluator-only0.5m global region suffices; GOAL_HOLD is optional.
M4v12 remains CLOSED_INCOMPLETE (9 arrivals among12 acquired runs;4 delay cases
unstarted), and no new matrix is released. No runtime is active. All work remains
uncommitted on feature/gesc-gaussian-robustness-v2 at3369cfc; no push or physical work.

Independent controls review_v2 verifies all1660 inputs/4980 fits and frozen gates
(67,437 assertions). The first review serialization failure is preserved; only
the cached review was corrected/repeated. Component review verifies1562 checks,
all48 fits/136 references and stable pins. See the validation record for hashes,
commands, review failure and calibration-window diagnostics.

[Next-method boundary audit](r20_method_boundary.md) distinguishes four recurrent
windows from four PDE windows: all are post-intervention and cannot alone prove
autonomous trapping. No R21 plan or runtime mode is adopted by this audit.


R20 material closure: context validator, diff check and existing phase checkpoint
PASS. Archive `checkpoints/r20_local_attraction_v1/` contains642 verified source
members, completed in1.312258089s; manifest SHA256
`2b5a65bb99ee17580004c62cba037dfbd03aab81eb0843788c5dfc37d1143349`.
This live receipt postdates the immutable archive. The study is COMPLETE and its
candidate REJECTED; source helpers remain unwired, full goal OPEN. No runtime,
new matrix, commit or push is active. All inputs, failed review-output attempt,
source/tests, fixed failures and independent reviews are retained.
