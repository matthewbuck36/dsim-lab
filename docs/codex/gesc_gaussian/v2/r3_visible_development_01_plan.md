# R3 visible combined-method development attempt 01

PROSPECTIVE, 2026-09-10. Simulation only. This plan prepares one fresh visible
case and releases it only after the current recurrent detector correction,
centered guidance revision, focused validation and interface overlay are stable.
It is development, not a historical M4 retry or an untouched confirmation case.

## Hypothesis and selected conditions

The retained nominal two-source D geometry is an exposed development condition.
The new recurrent detector should recognize the confined local trajectory;
centered continuous collection should then obtain spatially comparable raw
cycles, support the original negative-cost/ranking/fill guards, and allow the
existing Gaussian escape and ordinary continuous GESC to continue source seeking.
No source coordinates or evaluator geometry enter the new detector/guidance.

Use the existing one-case route template with fresh case/run/seed26091001 and
exclusive output root `development/20260910/visible_integrated_01/`. Select
`recurrent_geometry_v3`, `centered_tracking_v1`, `moving_cycle_coherence_v1`,
rolling GESC, .75m candidate radius/.15m comparison tolerance and the validated
.5/5 gain, .1m/s/.5rad/s Directional_Controller JSON. Fixed recurrent constants
include the separately calibrated30s/.04m static branch, two-arc circle branch
and harmonic oscillation branch. Keep old centroid heartbeat selector false;
new recurrent status heartbeat is intrinsic. Legacy methods remain selectable.

## Budgets and ownership

Stage A is180 simulated seconds and Stage B is120 simulated seconds after
actual local recovery. These replace the undispatched route template's120/60s
for this new attempt only. Retained B first detection104.8s plus20s collection,
5s design and35s escape is about164.8s; that motivates room to observe recovery,
without claiming the counterfactual D will follow that timing. Its saved first
SEARCH prefix ends68.6s without a recurrent candidate. A fresh42s recurrent
support/persistence plus collection and travel can also exceed60s after escape.
Maximum simulated exposure is300s. Recorder wall300s, scenario/outer process
wall340s, preflight45s and shutdown30s apply. Whole root attempt, including
cleanup and receipt publication, is bounded by420 wall seconds. The shared
nested cleanup deadline is root+390s and its45s shutdown/escalation reserve
limits effective work to root+345s at latest. Preflight and final recording
validation consume that allowance; full exposure/finalization is not guaranteed
if execution is slow. Such a timeout remains honestly INCOMPLETE. No fixed
simulation_duration_sec is selected because the staged monitor owns early stop.

Reuse existing run_scenario/record_run, GUI, subreaper_group_v3, strict full
recording validation, final-zero and cleanup. Establish the exact shared daemon
baseline before ownership using require_shared_daemon=True. Give nested scenario
and outer run_record_process the same root-relative cleanup deadline with30s
reserved for independent final cleanup/receipts. Save exact argv, source/overlay/
scenario pins, process ownership and final status. No concurrent Gazebo or
transport test may share the simulation domain. All old failed artifacts remain.

## Measurements and decision rule

Preserve the complete recording, resolved settings, recurrent model/guard/status
stream, all candidate/verification-stage/guidance/fill events and actual final
commands. Use current runner/recording/lifecycle owners for integrity, local
recovery, full fill/escape path and final goal assessment. Save detector support,
center, branch and first confirmation times; candidate approach time, raw-cycle
eligibility/rejection margins, admission/deadline, fill commit and escape timing.
Measure zeros during VERIFY/initial DESIGN separately from normal crossings,
safety/final stops and missing-companion intervals. Truth remains evaluation-only.

A candidate and centered motion alone do not pass the integrated hypothesis.
Require COMPLETE recording and scoped cleanup, valid typed lifecycle, successful
moving evidence/fill and local recovery to nominate further development. Final
source arrival/GOAL_HOLD is reported separately and required for eventual full
closure. Any source join failure is INCOMPLETE; any behavioral deadline/rejection
stays failed. Diagnose retained output before a separately changed attempt; no
unchanged retry or immediate matrix release.

After acquisition, use the existing single-run analyzer under a separately
recorded finite120s allowance, then fixed direction-reference targets through
existing normalized-input/reference owners if exposed. Save exact analysis
scope and command before reading the new bag. Analysis failures block comparison
release. Do not repeat a full bag merely to recover context; retain useful
normalized input and timeline tables for subsequent diagnosis.
