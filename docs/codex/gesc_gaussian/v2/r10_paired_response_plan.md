# R10: paired detector response on retained development inputs

ADOPTED prospectively2026-09-11UTC under the user's simulation method/development
authorization, after [R9](r9_motion_readiness_handoff.md) completed and its material
archive was saved. No runtime algorithm, old label, threshold or result changes.
This finite component study precedes any new comparison design or dispatch.

## Question and interpretation

V11's four global arrivals succeeded. Its original first-positive-mask latency
endpoints remain unavailable: brief mask visits do not establish independently
observed latency. The completed diagnosis found fragmentation close to the local
source. Selecting a favorable later residence, or using motion changed by VERIFY
to define pre-detection truth, would change that historical estimand.

Instead compare the methods' first would-confirm response on the same fixed,
uninterrupted observation. V9 B remained SEARCH until shutdown360.9s. This is
eligible observation, not independently proven trapping onset. Report response
from a fixed recorded SEARCH origin, supporting intervals and right censoring.
No hypothetical confirmation changes the input trajectory or the other detector.
This exposed trajectory supports a selected component response comparison;
it does not identify latent settling time, actual ROS scheduling latency or
establish the original30% basin-entry target.

Separately use the96 unchanged [R4 generating-model histories](r4_frozen_component_confirmation_plan.md):
36 positives,48 negatives and12 gray/withheld. Their labels follow the original
generators, not detector fits. Original finite recurrent results may be reused
only with exact core/dependency/config/input/schedule identity. They are exposed
development evidence in this new paired study, not new untouched confirmation.

## Owners, inputs and timing

Empirical input is only
`pilot/m4_pilot_v9/runs/2026-09-10/m4-pilot-v9-slot02-B-26090801/`
under `/home/mattb/Experiments/GESC-Gaussian/v2/`. Existing metadata confirms3610
`/pde_history` messages; the prior export omitted this topic. Reuse the existing
filtered bag reader once for the required PDE/state/pose/readiness/clock/timekeeper
streams. Preserve full recorded history and typed state at startup, before
readiness as well as within it. Reuse prior complete exports where identity permits.
Reject count/identity mismatches instead of truncating or reconstructing missing
PDE buffers. No other old run, holdout, Q1 or Q2 input is read.

Use actual `ConvergenceDetector.buffer_cb`, its reset/state/qualified-dwell owners
and the selected captured parameter values. Feed recorded flattened PDE histories;
do not replace the transport filter with pose windows. PDE's stationary route has
no readiness gate; preserve that difference rather than resetting it at readiness.
For recurrent geometry use the actual existing core with explicitly recorded
SEARCH/readiness admission, source stamps, frame/gap/reset rules and fixed
configuration. Clearly distinguish a positional core replay from full ROS
transport reconstruction. Bag ordering describes the replay schedule, not exact
historical callback causality. Save source/input coordinates and timing uncertainty;
a response difference within that uncertainty is inconclusive.

Use the first valid recorded SEARCH entry source stamp as the common empirical
origin. Retain any PDE confirmation before recurrent/readiness admission as
pre-admission/left-censored; never silently choose a later confirmation. Freeze
the replay sampling-resolution tolerance as the sum of maximum positive adjacent
source spacing in selected PDE histories and admitted poses plus maximum positive
selected clock spacing, over the fixed SEARCH exposure. Save all three terms.
This is a declared comparison-resolution rule, not a bound on unknown historical
callback delay. If the recorded source axes cannot be reconciled through existing
owners, the comparison is unavailable rather than aligned by a guessed offset.
For a method with no response, censor at its last actually evaluated source input
within the fixed SEARCH exposure; report SEARCH exit separately. Do not extend a
missing method endpoint across an unobserved tail. Use that observed endpoint for
any claimed advantage bound and reject responses outside the declared exposure.

For the R4 synthetic stratum, reuse each original source sample, including spikes
and irregular71/113/127/97ms intervals. Set ideal delivered ROS clock equal to the
source stamp. Reuse actual `PDEHistory._legacy_pose_cb` and publication path, with
the actual selected PDE detector callback; retain initialization, CFL clamping,
warmup, motion gate, decay, dwell and resets. This declared approximately10Hz
delivery stratum is not a reconstruction of Gazebo's callback schedule. Do not
resample or tune it. Recurrent cached outcomes use their original exact source
schedule and actual source-input confirmation coordinate, with model endpoint
reported separately. Preserve all96 cases and gray exclusions.

Hosts may replace only ROS clock access and publication sinks for offline calls;
no surrogate detection mathematics, ROS initialization or live publication is
needed. Review source bindings and focused adapter invariants before execution.
Save parameter origins and owner/helper/input hashes prospectively and verify
them after each job. Baseline misses and false confirmations are scientific
outcomes, not adapter failures. Preserve actual repeated PDE confirmations;
the response endpoint is the first confirmation.

## Finite execution and outputs

Exclusive root:
`/home/mattb/Experiments/GESC-Gaussian/v2/development/20260911/r10_paired_response_v1/`.
Prepare and review helpers before study execution. One focused adapter check
bundle has a60s inclusive ceiling. Then exactly two jobs, sequentially:

1. Empirical V9 B: one filtered reader pass and both declared responses,115s
   SIGINT plus5s kill allowance (120s inclusive). Retain input counts, readiness
   and SEARCH bounds, method gates, every completed/rejected recurrent support,
   PDE metric/reset/confirmation timeline, first responses and censoring.
2. R4 synthetic comparator: actual PDE callback replay for96 original histories
   and exact reused recurrent outcomes,175s SIGINT plus5s kill allowance (180s
   inclusive). Retain per-case response/censoring/counts, source sample hashes,
   CFL-clamp counts and all baseline false/missed confirmations.

Input/source checks and terminal child reaping belong within each inclusive
budget. No unchanged rerun after failure, no matrix or simulation dispatch.
Keep partial outputs/errors. Raw bags and all historical results remain unchanged.
Large numerical products stay external; repository validation records exact
commands, hashes, elapsed times, scope and findings.

The reviewed root launcher reserves preparation checks and receipt/reaping time
within those ceilings: child SIGINT at45/105/165s for checks/empirical/synthetic,
respectively, followed by2s kill allowance; outer envelopes stay60/120/180s.
Synthetic internal work stops at155s. These earlier cutoffs do not extend any
adopted ceiling. Preparation binds the exact selected command before each job.

## Prospective decision

Require complete, correctly bound paired analysis. Recurrent reliability must
retain all36 positives detected by120s, zero of48 negatives confirmed and the
one-confirmation latch; report baseline and gray outcomes separately.

For positive synthetic cases, summarize first-response times with every case in
the denominator: restricted waiting time is min(first response,120s), with an
explicit censoring flag. Report each class and the full36-case median/mean for
both methods; never compare successful-only medians. A practical speed benefit
requires lower recurrent median restricted waiting time with no recurrent
negative detections, plus earlier recurrent empirical response (or a supported
advantage bound when PDE is censored) beyond declared timing uncertainty.
No30% threshold is substituted for the original unavailable target.

If these checks pass, they support prospectively designing a newly scoped16-run
integrated arrival/direction comparison, alongside retained V11/R9 development
evidence. Such a comparison still requires working four-arm development analysis,
safe arrival, actual continuous acquisition, direction error/availability and
strict integrity before confirmation. If they fail or are inconclusive, retain
the result and make the next method decision from the evidence. R10 alone does
not release another matrix or close the full V2 goal.
