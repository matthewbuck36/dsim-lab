# R3 single retained C analysis timing

ADOPTED, 2026-09-10, following the completed
[cached normalization profile](validation/r3_cached_normalization_profile.md).
The original four-bag benchmark remains TIMEOUT. No historical contract,
acquisition, output, formula or allowance changes.

## One measured analysis

Exclusive root:
`/home/mattb/Experiments/GESC-Gaussian/v2/development/20260910/c_analysis_profile_v1/`.
Use the unchanged V9 contract as data and its original development slot3,
Arm C, `m4-pilot-v9-slot03-C-26090801`. Preparation inventories the declared
acquisition inputs and source bindings without opening bag bytes. The child
calls existing `_acquired` once, preserving its full input SHA checks, resolves
the identical science alias union, and calls the existing `read_run_bag` once.
Pass that returned BagData directly to existing `analyze_run_data` once.
Use the existing atomic exclusive writer for its mandatory independent-label
publication before event inspection. No other bag, reference/model, simulation,
full labels block, historical frozen-source gate or acquisition runs.

Thin wrappers record inclusive and nested self time, counts and raised errors
for existing labels/outcome owners, lifecycle validation, source validation,
policy indexing/pair checks, repeated envelope contract/configuration hashing,
objective snapshot extraction, direction adapter,
normalization, copying, canonical hashing and supplemental metrics. Time
existing outer lifecycle payload conversions by message type without wrapping
each recursive scalar conversion. No cProfile or deterministic line tracer is
used on this full analysis: its overhead would distort the previously observed
42.6-second interval. Instrumentation returns original objects and exceptions;
all overhead is inside the allowance.

Retain sparse stage progress, compact timings/counts, the actual metrics,
labels, selected topic record counts, lifecycle audit and qualification. Reuse
the one loaded bag throughout. Do not export another full normalized payload
or create an alternate extraction pipeline. For exact output parity, inside
the child read the cached C normalized JSON once, verify its retained digest,
and compare the entire returned document; verify new label bytes equal the
retained label digest. The cached JSON is input to this comparison only.

## Bound and release

Existing `finite_science_job`: 90 seconds inclusive, original 3-second cleanup
reserve (87 seconds useful work), `subreaper_group_v3`, 100-second outer shell.
All child imports, full receipt checks, one decode, analysis, instrumentation,
cached comparison, serialization/fsync and final checks belong to that bound.
Prepare source/input pins once under30 seconds after the separate runner
arrival/evaluator correction is stable. Hold the imported existing owners only
during the job. Dispatch waits for that owner's release and any active test or
analysis CPU work to end. Preserve failure/timeout; no unchanged retry or
expanded cap. Root owns overall milestone checkpoint/status.

## Decision

PASS requires normal completion, full normalized and label parity, stable
source/input identity, exactly one declared bag decode, complete original
receipt checks and cleanup. A profile can identify a dominant existing owner;
it cannot by itself establish corrected full-block throughput. Save any
minimal proposed correction prospectively, preserve baseline output/errors,
and test actual semantics/detachment before changing source. Avoid a broad
test campaign. If the hotspot is elsewhere, retain that finding without a
speculative copy removal or cache. The source-seeking research objective and
development-first comparison release conditions remain unchanged.
