# M4v13: comparison after complete recurrent-trapping development

ADOPTED prospectively on2026-09-11 UTC after R21 D02 independent cached review
passed34checks and material archive completed with661 verified source members.
Archive manifest SHA256:
3b184f33918c1ca3a7a0be60ecfed208cdba61f14c6e58395ca8c9becf797ec5
at checkpoints/r21_d02_complete_v1/. Source implementation/validation is the
current milestone. Preparation and dispatch depend on its passing evidence.
V12 stays CLOSED_INCOMPLETE; D01/D02 are separate exposed development and cannot
fill its unstarted slots. All roots below were absent before adoption.

## Question and evidence boundary

Compare four practical GESC + Gaussian packages after R21 D02 demonstrated a
complete noisy fill, escape, return to SEARCH and global arrival at197.18s,
continuous acquisition with zero mandatory stops, and direction median23.531 /
P9039.475 degrees with5/5 eligible averaging targets. The one completed case
supports a new comparison; it does not establish every condition or robustness.
D01's recording/analysis failures remain retained despite its separate arrival
and direction successes. No scientific evidence is rerun for context recovery.

The email's faster/reliable detector question has separate same-input component
measurements: R10 recurrent response102.008s versus PDE censored360.8s; recurrent
36/36 independent positive controls and0/48 negative controls, versus19/36 and
4/48 PDE. Retain the exact numerical detector and selected parameters. Carry
these finite, exposed component results through explicit source preservation.
Do not describe integrated arrival differences as isolated detector speed: the
trajectories diverge after intervention. The old independently labeled basin-entry
30% latency target remains UNACHIEVED/UNAVAILABLE secondary evidence. This plan
does not prove it, replace its estimand, or silently retire it from the overall
acceptance ledger. Report it explicitly at closeout.

Primary V13 outcomes are valid local recovery, post-recovery global arrival and
arrival time, continuous acquisition, and observed-phase stationary GESC direction
error/availability. GOAL_HOLD is optional under the user's arrival clarification.
Use exact recorded_pose_arrival_v1 for this version, preserving historical arrival
outputs. The0.5m region remains evaluator-only. Direction is not a universal
spatial-gradient guarantee. Preserve all native integrity, safety, final-zero,
cleanup, ownership, source-time and fill-cardinality requirements.

## Fixed population and runtime

Identity m4-pilot-v13; suite m4_pilot_v13; method
recurrent_trapping_integrated_arrival_v13; release policy
qualified_trapping_integrated_arrival_v1. Exclusive root:
/home/mattb/Experiments/GESC-Gaussian/v2/pilot/m4_pilot_v13/.
Source work root:
/home/mattb/Experiments/GESC-Gaussian/v2/development/20260911/m4_v13_source_v1/.
Both roots were absent before this proposal.

| Arm | Detector | Acquisition and verification |
| --- | --- | --- |
| A | existing PDE | inherited stationary sweeps |
| B | recurrent_geometry_v3 | existing stationary recurrent request |
| C | existing PDE | rolling GESC, moving-cycle coherence, centered verification, angular_profiles_v1 |
| D | recurrent_geometry_v3 | same rolling/centered direction package, authenticated recurrent_trapping_v1 verification |

D's new verification accepts authentic recurrent trapping under retained raw
quality/ranking controls; C retains its existing angular-profile rule. This is
an integrated package comparison, not a pure two-factor estimate of independent
main effects. No detector, estimator, controller gain, raw model, fill design,
escape or safety implementation changes are part of V13.

Reserve four visible primary nominal development cases, shared seed26091141.
Reserve twelve secondary confirmation cases, A/B/C/D order in each block:
nominal26091142; Gaussian sensor noise std0.015V seed26091143; sensor/pose delay
0.10s seed26091144. Keep starts, fields, half-gain V6 controller, speed limits,
noise units and disturbances from the arrival-family scenario. Select existing
controller-spawner load recovery and the R21 interface overlay. Preserve the
existing360s local-recovery/300s post-recovery stage deadlines and omit any fixed
minimum simulation-duration window.
Internal partition spelling holdout remains for compatibility; confirmation uses
fresh seeds on exposed conditions, not unseen geometries. Freeze source and all
sixteen configurations before dispatch. No replacements, retries, retuning or
unsealing after failure. Development4 and confirmation12 remain separate.

## Minimal existing-owner changes

Register the exact identity/method/seeds/release policy through m4_scenario.py;
add only explicit V13 schema-family selection. D alone selects the new policy
and existing wrapper topics. Keep all eleven primary predicates and retain the
first-path diagnostic as in V12. Preserve every historical version's output.

Extend m4_workflow.py's runtime and prerequisite binding for this exact version.
Require completed R10 decision/input/review/archive receipts, unchanged numerical
detector/core configuration, and the R21/D02 source, interface, component,
acquisition, analysis, reference, independent-review and material-archive chain.
R10's old recurrent_contract prefix must remain exact; the appended certificate
validator and changed lifecycle/launch bytes are justified only by completed R21
source and D02 evidence. Do not claim old launch/lifecycle byte identity.
Verify all D02 prepared scientific/runtime/helper pins remain unchanged except
the explicitly listed V13 orchestration owners: m4_scenario.py,
scenario_schema.py,m4_pilot.py,evaluate_m4.py,m4_workflow.py,run_m4.py. Version
source validation and the new freeze bind those current bytes. The originally
assigned future-version fixture updates are separately enumerated before source
validation: test_m4_v3_versions.py,test_m4_v4_versions.py,test_m4_v5_versions.py,
test_m4_v6_versions.py,test_m4_v7_versions.py,test_m4_v8_versions.py,
test_m4_v9_versions.py,test_m4_v11_versions.py,test_m4_v12_versions.py, under
ros2_ws/src/ros_esc/test/. Only the unsupported V13 sentinel advances to V14;
V11 also admits the now-known V13 family and its exact suite tuple. This explicit
fixture enumeration is recorded after those authorized fixture edits and before
validation; version_fixture_bridge.json binds old/new hashes and saved before
bytes. Before the two additional edits, also declare test_m4_v2_subreaper_routes.py
and test_m4_v3_shutdown_routes.py under the same directory: only their future
suite sentinel m4_pilot_v13 advances to m4_pilot_v14. No other old test pin is
exempt. Historical receipt
hashes and original result populations stay untouched. Missing, altered or
incomplete prerequisites block preparation/release.

Use existing run_m4.py preparation, acquisition, filtered labels, C/D references,
summary, release and report owners. Existing M4 labels already use one filtered
bag read and do not compute full native state durations; do not add D02's full
analyzer to each slot. Reuse captured noisy-configuration binding and the existing
motion owner. Select recorded_pose_arrival_v1 explicitly in evaluate_m4.py only
for V13. Extend m4_pilot.py/reporting only as required by the exact new protocol.
No new recorder, graph, analyzer, reference model or orchestration pipeline.

## Development release and confirmation decision

Require four COMPLETE acquisitions; all nine per-run science-completeness checks;
completed finite labels, both reference and summary jobs; exact24-target C/D
products; source/authority/cleanup and late nested hashes. In addition, both B
and D must complete valid recovery and global arrival; D must have
OBSERVED_CONTINUOUS_ACQUISITION, zero mandatory stopped acquisitions, and passing
numerical direction gates. These are prospective V13 requirements; V12's B OR D
rule and V11's latency-pair rule remain unchanged. Baseline A/C behavioral
failures remain valid measured comparisons and do not by themselves block release.
A missing or failed required development result closes V13 without confirmation.

Direction gates remain median<=30 degrees, P90<=60 degrees, usable averaging>=0.8
among eligible informative targets, with at least one eligible target. Keep all
24 targets at15+30k seconds, k=0..23, per C/D run. Report all exposure, eligibility,
availability, usable-output, averaging and paired counts. There are48 scheduled
development direction targets and144 confirmation targets. No selection based
on observed error. Require D's integrated arrival/continuous-motion/direction
outcomes in each confirmation condition for a successful combined-method claim.
Preserve per-arm/per-condition failures even when pooled direction passes.
Report all16 dispositions; planned confirmation denominator12 includes failures,
censoring, unavailable measurements and unstarted slots. Keep existing latency,
path length, direction jitter/lag and fallback outputs with their evidence limits.

## Finite validation and dispatch

One named focused source bundle:230s work+5s termination,260s inclusive. Cover
all16 selections and exactD wrapper policy, V12/V11 parity, recorded-arrival
selection, complete/missing/altered prerequisite chains, late mutation, B-only and
D-only release rejection, zero/unknown/observed stops, failed D direction,
complete target populations and historical censored-latency semantics. Reuse
existing fixtures and relevant M4/R21/arrival/motion regressions. Preserve any
failure before a separately documented bounded correction; no whole-workspace
campaign. Archive before source copies, actual diff, exact command and results.
Verify the current21 installed entrypoints and R21 generated interfaces. No build
is needed for Python version/orchestration changes. Independent source review,
context/diff checks and material checkpoint precede preparation.

Preparation through the existing owner has600s maximum; audit the resolved16-row
contract and source bindings before dispatch. Source R21 runtime_environment.sh
with initially clean PYTHONPATH/RMW, single BLAS threads, domain201,localhost1,
DISPLAY:0 and preserved shared daemon. All runtime uses simulation clocks.

Keep720s recorder,900s inclusive case,45s shutdown grace and30s independent
cleanup. Four-run block labels240s, each C/D reference45s, summary10s. Freeze and
report20s each; science total1400s. Suite15800s inclusive (4h23m20s), rootSIGINT
15680s plus120s kill. Report actual elapsed time; these are ceilings. Four visible
development cases precede a single automatic release; confirmation is a repeated
batch under the existing rendering policy. Stop on required infrastructure or
integrity failure and retain every outcome. No physical/Pi/snapshot/V1/commit/push.

## Source fixture correction, adopted before edits and a second validation job

The first fixed bundle is retained:637passed/3failed out640,144.097535851s,
961stable pins and unchanged21 installed entrypoints. Three new fixture mistakes
are isolated: an absent optional resolved execution mapping; missing direction
rows after a deliberately failed reference job; and a summary-only B binding
mutation rejected by the earlier label/summary integrity join.

Correct only test_m4_v13_versions.py and test_m4_v13_workflow.py. Use the absent
execution mapping's empty default; preserve missing reference rows as unavailable;
make the B binding fixture update both label and summary witnesses consistently
so it reaches the explicit V13 recorded-arrival guard. Do not change production,
measurement, release, safety or scientific thresholds. Preserve original tests,
JUnit failures and receipts.

The existing preparation/archive/release helper receipt selectors advance from
focused_v1 to focused_v2 before validation. Allow one second identical640-case
focused selection,230s work+5s termination/260s inclusive. This reuses the existing
validator and produces one complete passing selection on the actual current
source/helpers if successful; no new aggregate-validation owner or merged PASS
receipt is introduced. Any failure stays retained and blocks preparation.
No empirical comparison has started or been retried.
