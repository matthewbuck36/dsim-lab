# M4 v4 source validation handoff

M4v4 canonical-publication correction is CLOSED_SOURCE_VALIDATION_PASS,
2026-09-10UTC. Full V2 remains IN_PROGRESS. Read its
[amendment](m4_v4_confirmation_mirror_plan.md),
[validation](validation/m4_v4_confirmation_mirror.md), newest status and any
later preparation/acquisition receipts before acting. No new acquisition has
started at this source boundary; old M4v1/v2/v3 remain failed and immutable.

The selected moving/PDE detector now publishes one exact post-decision
canonical status before confirmation or resets. Stationary order is unchanged;
binding and strict validator bytes are unchanged. The actual callback baseline
failed once; corrected focused and actual DDS fixtures pass, including two
epochs and serialized lifecycle validation. Missing/altered mirrors still fail;
valid confirmations with no candidate snapshots remain unavailable moving
evidence rather than accepted fills.

Final1042 unique checks PASS172.63s,618 source pins stable,21 installed Q5
entrypoints verified; actual installed CLI parsers pass. External evidence root
`builds/m4_v4_confirmation_mirror_v1/` under the V2 experiment root contains
`root_final_v1_receipt.json`, exact logs/JUnit, source scopes, preserved baseline,
version and consumer holds. No numerical/control/geometry/gate change occurred.
Only seven previous source/test pins changed;608/615 prior files are identical.
The new amendment and two tests add three pins. All169 oldpilotfiles and three
closure archives independently verified unchanged without scientific reanalysis.

Fresh exact `m4-pilot-v4` uses existing identities and group_v3 ownership,
methodv1, the unchanged16slots/192targets and600/720/900/15300s caps with900s
science. B/D retain Q7two-block6s/.18m/.50m; C/D retain Q2coherence.75 and all
M3 safeguards. Holdouts require four complete safe development cases plus
attempted science and complete outcome ledger; scientific failure is retained.
No post-freeze tuning/replacements or importing earlier runs.

Next: source archive, one preparation, independent frozen audit and exact
dispatch release in clean Q2thenQ5 overlays, domain184/localhost1. Check for
existing v4 preparation/acquisition first; never repeat fixed evidence. Source
is held. Git remains feature/gesc-gaussian-robustness-v2 at3369cfc, task edits
saved uncommitted. No physical/Pi, V1, commit or push action occurred.
