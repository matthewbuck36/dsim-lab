# R3 visible combined-method development attempt02

PROSPECTIVE 2026-09-10. Preserve [attempt01](validation/r3_visible_development_01.md)
which failed graph preflight before readiness. This is a new development identity
with a bounded startup-routing correction, not an unchanged retry.

The hypothesis, exposed nominal geometry, seed26091001, numerical methods and
180/120 simulated-stage,300 recorder-wall,340 process-wall,30 shutdown and420
root-wall budgets are identical to [the adopted attempt01 design](r3_visible_development_01_plan.md).
No acceptance gate or old result changes. Only explicitly admit recurrent robust
rolling simulation in the shared stationary-selector helper and source-verify
the actual GaussianFill/SupervisorNode constructors before freezing02. Retain
rejection of stationary recurrent, physical, legacy or unknown combinations.

Use exclusive external `development/20260910/visible_integrated_02/` and
case/run `v2_method_development_D_20260910_02`. Freeze corrected source, actual
schema2 interface overlay, environment/domain201/local-only/GUI and final
scenario before dispatch. Use the existing finite wrapper/runner/recorder and
strict daemon/cleanup owners. No new full-source campaign or interface build is
necessary for this pure routing correction. One material phase checkpoint and
focused actual-constructor regressions suffice; source snapshot01 remains.

Use the prepared existing-owner post-run analysis with its honestly declared
physical read count after a meaningful recording. Any failed readiness, joins,
lifecycle or behavioral deadline remains failed/incomplete. Preserve every
artifact and diagnose before further changes. Full comparison remains blocked
by the retained labels throughput timeout and missing integrated evidence.

Pre-release read-only consumer audit also found the new detector configuration
AlgorithmEvent detail was not recognized by the existing producer classifier.
Add the explicit recurrent detail to that classifier's known detector set,
keeping unknown modes rejected. Focused actual-event classification must pass
before02; this repairs producer attribution rather than waiving the gate.
