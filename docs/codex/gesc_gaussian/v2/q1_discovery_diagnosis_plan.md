# Q1 discovery diagnosis — 2026-09-09 UTC

Status: ACTIVE bounded diagnostic after the closed Q1 result. Original
`q1_plan.md`, acquisition, labels, settings and sealed confirmation remain
unchanged. This development work does not nominate a setting or release M4.

The frozen residence labels are19.924s then29.716s, with uninterrupted eligible
pose input through both. All nine original settings emitted zero flags. Test
two distinct explanations: actual motion leaves the certified near-minimum
region, and the fixed6s mean windows may still follow that motion strongly.
No new acquisition, field recomputation, threshold sweep or confirmation
inspection is authorized by this diagnostic.

1. Extract spatial label durations, eligible source chains, actual trajectory
   extent and recorded region geometry from the two frozen discovery label
   artifacts. Keep holes/unknowns intact. Save one bounded60s exclusive
   diagnostic under recovery4 `diagnostics/discovery_residence_v1/`.
2. Instrument the unchanged `CentroidWindowDetector` at the original W6s,
   epsilon0.30m and largest original R0.75m only. Feed exactly the frozen
   eligible poses with the same epoch/generation admission used by Q1; record
   every completed six-window score, five component shifts, centroids and
   radius. No candidate search, parameter changes or M3 re-evaluation. The
   purpose is to explain zero events, not repeat the nine-setting experiment.
   Save one bounded60s exclusive diagnostic under
   `diagnostics/discovery_detector_response_v1/`. Include all completed windows
   from both discovery inputs and report early/late behavior without choosing
   a favorable subinterval. Hash inputs and runtime source before/after.
3. Review the independent label definition against the intended positional
   settling claim. Identify what the existing certified sublevel cells prove,
   and what they do not prove. Preserve the old oracle and result. Use the
   measured trajectory and fixed-score decomposition to select a concrete
   next hypothesis in a new prospective amendment, if warranted.

No Q1 direction reference is run: its original confirmation-target prerequisite
is unmet. Direction diagnosis, changed temporal definition or new region
qualification would require a separately declared scope and evidence version.
Record exact scripts, commands, outcomes and limitations; inspect the diff and
checkpoint Q1's closed boundary before further runtime changes.
