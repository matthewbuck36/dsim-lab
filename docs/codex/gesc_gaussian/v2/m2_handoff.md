# M2 implementation and finite-evaluation handoff — 2026-09-09 UTC

M2 implementation is complete within its source/test scope. Its fixed
historical reference attempt concluded **EVIDENCE_UNAVAILABLE**; direction
research qualification is not achieved. This is not full-plan completion.

## Implemented and checked

Existing source/encoder/transform/delay/composer/filter/runner/recorder owners
now implement the opt-in schema2 acquisition-time contract, atomic objective
identity, synchronized observed world phase, time-weighted rolling GESC,
three-cycle confidence, fresh instantaneous fallback and explicit invalidation.
Legacy runtime defaults and public array layouts remain. No physical tree,
mount, device or hardware action was used.

The source-clock amendment is authoritative over the earlier publication-key
assumptions: `m2_source_clock_correction.md`. Actual source acquisition, cost
publication, original per-component callback receipts and synchronization
admission remain distinct. Source-clock lag cannot manufacture future output;
late packets cannot restore a disputed key. Intentional source-key revocation
still quarantines ambiguous recordings under the existing strict validator;
runtime recovery is a separate property.

Final validation records preserve exact commands, failures and hashes:

- `validation/m2_clock_admission.md`: interfaces build15.4s; final integrated
  433 PASS/1 inherited quaternion warning in9.27s, domain175/timeout180s.
- `validation/m2_clock_wiring.md`:46 schema/launch plus8 selection checks pass.
- `validation/m2_upstream_clock.md`:61 owner-callback/source/brightness checks
  pass in1.89s, including source conflict notification and original-age expiry.
- `validation/m2_upstream_transport.md`:2 full upstream/default-legacy DDS tests
  pass in8.00s, domain176/timeout120s.453 ordinary acquisitions share151 actual
  publication ticks;186 observations qualify after3cycles. One fresh source
  recovers instantaneous fallback after2deliberate invalidations. Three
  inherited unused-quaternion warnings are retained; checked geometry is finite.

No Gazebo, new field reference calculation, historical replay retry or physical
operation ran during this correction. Test counts are checks/observations, not
independent experimental trials or direction-performance statistics.

## Preserved scientific outcome

`validation/m2_reference_v1.md` and its manifest close the finite192-slot
historical reference attempt. All8 input traces rejected duplicate conflicting
timestamps; all192 anchors remain unavailable. Error/availability values are
null, not measured0%. Source forensics and the correction do not repair those
recordings' missing acquisition provenance. The13historical holdouts remain
unopened. A prospective same-trajectory reference version is still needed.

M1/M1a likewise concluded without a selected calibrated detector neighborhood.
Do not treat synthetic defaults or tests as calibration, or convert closed
failures into qualified evidence. No16-run pilot is released by this handoff.

## Recoverable source boundary

Branch `feature/gesc-gaussian-robustness-v2`, HEAD
`3369cfc83a64ff5d8354827fd5310caaf0c8e945`; current source edits are uncommitted
and task-owned. V1 remains closed at1af67c6. The context validator and checkpoint
tool passed before this handoff.

Material source/archive boundary:
`/home/mattb/Experiments/GESC-Gaussian/v2/checkpoints/m2_clock_boundary_v1/`.
It contains93 changed/untracked source/evidence files in a verified531542-byte
archive, exact staged/unstaged binary patches, Git state and per-file hashes.
Manifest SHA256:
`3039aeda1ebbe9b9cc447d3cf002254a88a7160aa6c8bfed53cf26443715fb4f`.
This handoff and subsequent status updates were written after that archive;
its source/test boundary remains immutable.

## Next independent work

Freeze M3's authoritative SEARCH-epoch/confirmation binding, phase/trajectory
comparability, uninformative-source veto and candidate-bound fill transaction
contract before source edits. Independent lifecycle/controller tests can use
explicitly synthetic neighborhoods. Calibrated integration, prospective
same-trajectory qualification and pilot release remain separate open gates.
