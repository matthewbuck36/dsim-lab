# Q2v2 primary qualification handoff

The dated-directory source correction and four fresh simulation acquisitions
completed. Detector qualification remains EVIDENCE_UNAVAILABLE; the direction
branch completed as a discovery-only diagnostic. Confirmation scientific
outputs remain sealed, and this result does not release M4 or complete V2.

The correction uses the existing recorder layout and unique exact-run lookup,
with explicit Q2 version/identity validation before input reads. All runtime,
scientific label, numerical reference and acceptance owners stayed unchanged.
Validation passed122 path/contract checks,102 acquisition checks across the two
required environments,92 study checks and210 old-route regressions. The installed
scenario build and binding passed. Exact commands, versions and hashes are in
`validation/q2_acquisition_path_correction.md`.

The single acquisition admitted4/4 new inputs, each125 simulated seconds after
readiness. All61 completeness checks per run and spawn/final-zero/cleanup gates
passed. Acquisition took639.006260s (wrapper641.103203s), exit0. No input from
the failed21-series attempt was imported; that attempt stays CLOSED_INCOMPLETE.

The single label job completed with no integrity errors. Neither discovery run
contains a genuine positive residence episode, supported42s opportunity or
censored positive episode. All nine residence evaluations and their aggregates
are EVIDENCE_UNAVAILABLE; all nine approach rows pass17.816s eligible negative
support. There are zero detector events and zero M3 evaluations. No detection
latency, sensitivity or moving-verification result is available. The42s gate,
certified annular region and excluded hole remain unchanged.

The predeclared single reference job processed only24 discovery targets; the
other24 planned slots remain symbolic and sealed. Actual75% output was usable
and averaged at24/24 eligible informative targets, with no fallback/weak output.
Its pooled median angular error is31.664092deg and p90113.393071deg; aligned
instantaneous median is58.560725deg. Median improvement is descriptive, while
the large upper tail warrants diagnosis. Direction and combined qualification
are NOT_EVALUATED. This is neither a new weight nomination nor whole-run
availability or held-case performance evidence. Both scientific jobs exited0.

`validation/q2_qualification_v2_result.md` records immutable acquisition, job,
nomination, reference and independent audit receipts. Artifacts live under
`/home/mattb/Experiments/GESC-Gaussian/v2/qualification/q2_primary_shadow_v2/`.
Source/configuration stayed frozen through this boundary. Retain all failed,
unavailable and diagnostic results; no fixed experiment or model job is rerun.
Immutable closure SHA256
`51d73de197aa24f2aba8cb9f1eac576aaaa38e7519d6abc672f5113a9acfa137` retains120
artifact hashes and573 unchanged source receipts. Material archive
`checkpoints/q2_v2_qualification_closed_v1/manifest.json`, SHA256
`014149d7d4a3f91a7f4797f43429ea13cadd3cf893645b88367c3ea21ee66d28`,
contains265 files, a1098058-byte verified archive and241 retained hashes.
Context/diff/checkpoint checks passed; these receipts were appended after the
immutable archive with no runtime/scientific source change.

Next work in `q2_discovery_motion_diagnostic_plan.md` is a bounded discovery-only diagnosis of
the motion, detector support and saved direction errors. It may explain missing
exposure without changing its label. Any new positional-confinement study needs
a separate definition and claim; it cannot retroactively turn this study into
a pass. No confirmation inputs or alternative weights are authorized by this
closeout. Keep the original16-run M4 comparison and full acceptance ledger.
The recorded Arm B centroid/stationary compatibility gap and separate composer
future-clock admission concern also remain before M4.

Branch remains feature/gesc-gaussian-robustness-v2 at HEAD/origin3369cfc, with
task-owned dirty source/evidence. No new commit/push, V1 change, physical source
edit, Pi access or hardware action occurred. The full implementation goal is
active and incomplete.

## Subsequent diagnostic boundary

The saved-data motion report and recorded centroid extraction are now complete:
`validation/q2_discovery_motion_diagnostic.md` and
`validation/q2_recorded_detector_diagnostic.md`. Both trajectories loop near the
local source; all20 complete recorded snapshots fail score. Residence also has
state-related detector resets, separately from continuous admitted pose/policy
history. No detector, model or fixed scientific evaluation was rerun.

Checkpoint `checkpoints/q2_v2_discovery_diagnostics_closed_v1/manifest.json`,
SHA256 `add832e4fd397e1bf8b643a0b12784190fc243b47b260ee0e77bebcb73953745`,
retains272files/1111223-byte archive/264 artifact hashes before further source
changes. `q3_detector_clock_admission_plan.md` is the active bounded source
correction, following three retained failing actual-owner baseline tests.
Scientific sensitivity, direction qualification, the Arm B adapter and M4 remain
open. The original Q2 closure and confirmation seal are unchanged.
