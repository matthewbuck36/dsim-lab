# M4v14: retained development and twelve fresh confirmations

ADOPTED prospectively on 2026-09-11 UTC after root review of the current owners
and R22's completed source, measurement, independent review and material archive.
Source implementation and focused validation are now the active milestone;
preparation and dispatch depend on the passing evidence specified below. It follows the
[R22 completed handoff](r22_readiness_command_pairing_handoff.md) and preserves
[V13 CLOSED_INCOMPLETE](m4_v13_handoff.md). Root owns adoption and subsequent
bounded job releases; no permission question or implicit dispatch is introduced.

R22 material archive: `checkpoints/r22_readiness_command_pairing_closed_v1/`,
673 individually verified source members, manifest SHA256
`21385d3af80c3818b581475cbc26db3cb7c381d35eaf488fc394558365a34049`.
V13 closure archive: 669 members, manifest
`f393c5ab1786127c84b17fa5a35b4d95e370b616b172b1a3c2e8e4d2d0b21e58`.

## Purpose and evidence boundary

Complete one sixteen-record integrated comparison using the four already exposed,
complete V13 development acquisitions and twelve newly reserved confirmation
acquisitions. No development simulation, bag scan, position-label computation,
direction normalization or numerical reference is repeated. Original development
identities, configurations, observations, measurements and failures stay immutable.
The new comparison cites the originals and R22's separately qualified C/D motion
measurements; it does not unseal V13 or populate V13's twelve UNSTARTED slots.

The primary outcomes remain valid local recovery, evaluator-only global arrival
and arrival time, continuous acquisition, and observed-phase stationary GESC
direction accuracy/availability. The 0.5m arrival region and exact
`recorded_pose_arrival_v1` binding remain unchanged; GOAL_HOLD is optional. Retain
all eleven primary success predicates, with first-verification path diagnostic.
The comparison evaluates integrated packages whose trajectories diverge after
intervention. It does not isolate detector speed or independent factorial effects.

Carry forward R10's exact, finite same-SEARCH response and known-model positive/
negative evidence through its existing source/configuration/receipt chain. These
results do not prove the original >=30% latency reduction from independently
labelled basin entry. That endpoint remains open, unachieved/unavailable secondary
evidence unless the unchanged six-pair owner measures it in the fresh confirmation
population. Do not substitute absolute confirmation time, global-arrival time,
R10's response reduction or a later favorable residence for the original endpoint.

## Fixed comparison identity and population

New experiment identity: `m4-pilot-v14`; suite `m4_pilot_v14`; new-document method
identity `recurrent_trapping_integrated_arrival_v14`; release policy
`retained_development_integrated_arrival_v1`. The method suffix versions this
comparison/measurement protocol; no numerical runtime method changes are allowed.
Original source artifacts keep their V13 experiment/method fields unchanged.
Exclusive comparison root:
`/home/mattb/Experiments/GESC-Gaussian/v2/pilot/m4_pilot_v14/`.
Exclusive source/preparation work root:
`/home/mattb/Experiments/GESC-Gaussian/v2/development/20260911/m4_v14_source_v1/`.
Verify both roots are absent before their respective first creation; never
repurpose a populated root or an attempted slot.

| Arm | Detector | Acquisition and verification |
| --- | --- | --- |
| A | Existing PDE | Inherited stationary sweeps |
| B | recurrent_geometry_v3 | Existing stationary recurrent request |
| C | Existing PDE | Rolling GESC, moving-cycle coherence, centered verification, angular_profiles_v1 |
| D | recurrent_geometry_v3 | Same rolling/centered package, authenticated recurrent_trapping_v1 verification |

| Comparison slots | Origin | Geometry / condition | Seed | New dispatch |
| --- | --- | --- | ---: | --- |
| 1–4, A/B/C/D | Retained V13 development | Primary / nominal | 26091141 | None |
| 5–8, A/B/C/D | Fresh V14 confirmation | Secondary / nominal | 26091152 | Four |
| 9–12, A/B/C/D | Fresh V14 confirmation | Secondary / Gaussian sensor noise, std 0.015V | 26091153 | Four |
| 13–16, A/B/C/D | Fresh V14 confirmation | Secondary / sensor and pose delay 0.10s | 26091154 | Four |

The retained run IDs are exactly:
`m4-pilot-v13-slot01-A-26091141`, `m4-pilot-v13-slot02-B-26091141`,
`m4-pilot-v13-slot03-C-26091141`, `m4-pilot-v13-slot04-D-26091141`.
Their case IDs, source run directories, source experiment/method identities,
resolved scenarios, actual launch/controller configurations, typed stream IDs,
Timekeeper origins and visible acquisition records remain unchanged. Do not
manufacture a V14 runtime ID for old messages. A comparison slot is a reference
to its declared source acquisition, not a new acquisition identity.
Fresh run IDs use `m4-pilot-v14-slotNN-ARM-SEED` and existing case-ID generation.
Keep all original starts, fields, bounds, V6 half-gain controller, speed ceilings,
sensor geometry, detector settings, fill/escape behavior and R21 overlay. Select
existing spawner recovery. Preserve 360s local-recovery and 300s post-recovery
stage limits, with no fixed minimum simulation-duration window. Fresh seeds and
new output/run identifiers are the only intended condition-specific differences.

The original four development runs were visible. There is no new visible
development probe; the twelve confirmations use the existing batch rendering
policy. Internal partition spelling `holdout` remains compatible; these are fresh
seeds on already exposed conditions, not unseen geometries or independent new
development. Freeze all twelve configurations before their first dispatch.

## Exact retained-development receipt and allowed measurement composition

Create one new immutable reuse receipt through the existing workflow owner. Bind
by exact path/SHA the V13 contract and closure archive; four native acquisitions,
all original non-bag recorded-input receipts and inherited native raw-bag hashes;
the completed original labels, references_3, references_4 and summary job receipts,
logs and source identities; four per-run labels/metrics, block labels/result and
block receipt; both qualified normalized and 24-target reference products; and
the original release failure/final ledger. Require native recording, source/input
integrity, inner/outer cleanup and actual acquisition authority to be complete.
A's observed behavioral failure remains present and is not an integrity failure.

Bind R22's capture/preparation/execution/independent review, exact before-source
copy and evaluator patch, 91-test source-validation receipt and static review,
cached component preparation/execution/result/receipt, both historical default
objects, both selected motion objects, independent 39-check review and closure
archive. Require exact reproduction of both original motion objects, unchanged
common C motion fields, qualified selected C/D results, unchanged intervention
authority and all nested receipts. Use R22's retained input identity; no new
raw-bag scan/hash pass is needed to build this cached reuse receipt. Explicitly
label inherited raw SHA provenance and check raw file size/mtime/inode/device;
this is not a claim of freshly recomputed bag hashes.

Save a separate V14 development measurement view. For C and D only, its exact
allowlist of derived field changes is:

- `mandatory_stop_evidence`, replaced by the bound R22 selected object.
- `mandatory_stopped_acquisitions`, copied from that same object.
- `scientific_analysis_checks.motion_measurement_complete`, derived from the
  selected object's `analysis_complete`.
- `scientific_analysis_complete`, recomputed as the conjunction of the original
  nine named checks after that single check substitution.

Recompute the block's `four_arm_analysis_complete` and final conjunction using
these views and the unchanged `both_reference_products_complete` evidence. Retain
original and effective values with their separate receipts. Every other field,
including latency/labels, errors, arrival, geometry, direction rows and summaries,
raw qualification, first-path diagnostic, path length and actual outcome bindings,
must equal its original. Do not edit old label, normalized, reference or summary
files, or change their version/source headers to satisfy a new validator.

The new comparison envelope carries its own identity and explicit source-slot
mapping. Existing validators may accept mixed source experiment identities only
for the four exact declared retained slots; all fresh slots use strict V14 IDs.
A/B remain byte/field-identical measurements. Generic cross-version acceptance,
arbitrary imported slots and user-selected metric replacement are out of scope.

## Existing owners and source preservation

Implement through exactly these existing production owners:

1. `ros2_ws/src/ros_esc/ros_esc/scenario_runner/m4_scenario.py`: register V14,
   exact population/source-slot mapping, method/release identity and unchanged
   execution budgets; preserve every historical version's serialization.
2. `ros2_ws/src/ros_esc/ros_esc/scenario_runner/scenario_schema.py`: only selected
   V14 family/retained-slot contract support; fresh cases retain current runtime
   routes and predicates. No runner or algorithm source change.
3. `docs/codex/gesc_gaussian/v2/tools/run_m4.py`: admit authenticated retained
   development through the existing workflow callback, save a truthful reference
   ledger, perform the one release and launch only slots5–16. Do not create old
   run-directory copies or pretend inherited executions occurred in this job.
4. `docs/codex/gesc_gaussian/v2/tools/m4_workflow.py`: own the reuse receipt,
   strict source bridge, cached development view, selected identity validation,
   release, fixed fresh block analysis and final report. The retained branch
   validates original successful jobs against their original argv/source/path
   receipts; it never fabricates four new labels/reference/summary jobs.
5. `docs/codex/gesc_gaussian/v2/tools/evaluate_m4.py`: for fresh V14 C/D only,
   forward `pairing_basis='full_publication_order_v1'` to the existing motion
   owner. Preserve explicit recorded-arrival selection and historical defaults.
   No new analysis pipeline or development bag-read route.
6. `ros2_ws/src/ros_esc/ros_esc/plotting_scripts/m4_pilot.py`: exact retained-slot
   identity admission and V14 reporting selection; reuse existing calculations,
   direction/product checks and `integrated_method_run_checks`.

The evidence bridge has two explicit steps. First, V13 evaluator bytes to the
completed R22 evaluator (`f6369cdd6a3d659cab5f10997945945050e868140f91bfadd09aeb5f54f6765c`)
are justified only by the saved R22 before/patch/tests/component chain; the
historical default remains unchanged. Second, preserve before copies of the six
listed current owners and bind the exact prospectively scoped V14 orchestration
patches and source validation. No broad dirty-file or whole-directory exception.
All runtime, numerical detector/direction/model, interfaces, selected configuration
and unrelated historical helper sources remain exact. New measurement selection
is an offline contract change, not a controller or detector change.

The permitted existing future-version fixture changes are exactly these files
under `ros2_ws/src/ros_esc/test/`: `test_m4_v3_versions.py`,
`test_m4_v4_versions.py`, `test_m4_v5_versions.py`, `test_m4_v6_versions.py`,
`test_m4_v7_versions.py`, `test_m4_v8_versions.py`, `test_m4_v9_versions.py`,
`test_m4_v11_versions.py`, `test_m4_v12_versions.py` and
`test_m4_v13_versions.py`, `test_m4_v2_subreaper_routes.py` and
`test_m4_v3_shutdown_routes.py`. The last two contain the same unsupported suite
sentinel in the existing shared evaluator-route fixtures. Advance unsupported
version/suite14 sentinels to15; V11/V13
membership fixtures admit only the newly supported14 identity. Save exact old/new
hashes and before bytes in the fixture bridge before validation. No historical
behavior assertion or actual output is weakened. Any additional fixture change
requires a specific prospective addition to this list before editing; other tests
remain unchanged. Verify the same 21 installed entrypoints and R21 generated interfaces.
The original V13 `verify_frozen` and failed release remain fail-closed under the
changed current source; do not relax them to import old results. New-version
receipts explicitly authenticate the historical source and the approved bridge.

## Unchanged development gate and final claims

Require four complete native acquisitions and all nine per-run measurements,
with the narrowly composed C/D motion values above, both exact 24-target reference
products, original authority/cleanup, and stable nested receipt checks. Require
both B and D valid local recovery and recorded global arrival. Require D
`OBSERVED_CONTINUOUS_ACQUISITION`, integer zero mandatory stopped acquisitions,
complete pairing/authority, and numerical direction PASS through the existing
shared check. C must have complete scientific measurements; baseline A/C
behavioral or direction failures remain measured outcomes, not substituted passes.
Do not lower these requirements because development has already been observed.

Direction thresholds remain median <=30deg, P90 <=60deg and usable averaging
>=0.8 among eligible informative targets, with at least one eligible target.
Retain all 24 targets per C/D run at 15+30k seconds for k=0..23: 48 exposed
development targets and 144 fresh-confirmation targets. No favorable subset
selection. Report D's combined arrival/recovery/continuous/direction outcome
separately for each confirmation condition; pooling cannot erase a condition's
failure. Retain all arms, all sixteen dispositions, missing/censored/unstarted
entries, wrong fills/goals and original six latency pairs/12 endpoints.
Development reuse does not enlarge the fresh-confirmation denominator.

## Finite validation, preparation and dispatch

After adoption, one named focused source bundle: 230s work plus 5s termination,
260s inclusive including source/entrypoint checks and receipt retention. Reuse
existing V13 workflow/version/metrics/arrival fixtures and R22 motion regressions.
Add meaningful cases for exact retained identities and source receipts, allowed
measurement-field composition, altered/stale/missing provenance, attempted slot
reuse, zero development launches/exactly twelve fresh launches, unchanged A
failure and C motion, missing/failed B or D arrival, unavailable/observed stops,
failed D direction, strict full pairing faults and historical default/version
parity. Keep complete24/48/144 target populations. No whole-workspace suite.
Preserve a failed boundary before any separately authorized bounded correction;
no automatic repeat or test gate weakening. Archive source before copies, actual
diff, command, receipts and an independently reviewed checkpoint before preparation.

One existing-owner preparation job, maximum600s inclusive (595s SIGINT plus5s
kill), freezes the new
contract, original source-slot references and twelve fresh resolved cases. Reuse
qualified cached development/topology/configuration receipts when their complete
input and numerical-source identities match; normal existing validation of fresh
resolved conditions remains required. This job performs no development bag,
labels, direction normalization or reference evaluation. Save exact preparation
inputs/outputs and audit all sixteen source-slot bindings, selected routes,
seeds, budgets and the twelve unattempted fresh slots before release.

One cached development receipt/summary validation job, maximum60s inclusive
(55s SIGINT plus5s kill), hashes retained non-bag products, verifies the narrowly
allowed composition and invokes existing summary/release checks. No bags, model
calls, fits or scientific replay. Independent bounded cached review must pass.
A missing or altered prerequisite, unsupported identity, unauthorized source
change or required development failure stops the new comparison before any
confirmation launch. Preservation of the original failed release is mandatory.

Keep existing720s recorder,900s inclusive case,45s shutdown grace and30s independent
cleanup; four-run fresh block labels240s, each C/D reference45s, summary10s;
freeze/report20s each. Retain the existing15800s inclusive suite ceiling and1400s
science reservation; the unused development case/science allowance is slack,
not authorization for more executions. Record the actual smaller acquisition
population and elapsed time. Root outer SIGINT15680s plus120s kill retains the
existing maximum. The dispatcher must account for only twelve actual case
attempts and three new scientific blocks, with cached development recorded
separately, and issue one automatic release before the first fresh slot.

Use the existing R21 runtime environment with initially clean PYTHONPATH/RMW,
single BLAS threads, domain201, localhost1, DISPLAY:0 and current shared daemon;
preserve simulation clocks and existing batch rendering. Every attempt and
partial product is exclusive and retained. Stop on required integrity/process/
recording failure; report measured behavioral failures without replacements.
No new development simulation, retries, slot substitution or post-freeze tuning.
No physical/Pi/snapshot/V1/commit/push. Closeout requires independent result review,
exact validation evidence, context/diff checks, material checkpoint, handoff and
all remaining research limitations, including the original open30% endpoint.
