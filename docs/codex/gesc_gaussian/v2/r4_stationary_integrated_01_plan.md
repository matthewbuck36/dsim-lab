# R4 stationary recurrent integrated development01

PROSPECTIVE, 2026-09-10. Preparation follows the independently validated
[stationary pairing](validation/r4_stationary_recurrent_pairing.md). Dispatch
requires its final launch review and the bounded scenario/evaluator correction
below to pass, followed by source/scenario freeze. Authority is the user's
[method development](method_development_20260910.md) and
[global arrival criterion](global_arrival_acceptance_20260910.md).

## Hypothesis and scope

Pairing the unchanged recurrent detector with the existing stationary
verification/fill method should produce a typed local candidate, authorized
stopped verification, one fill and recovery into SEARCH. Observe subsequent
global arrival under the existing evaluator-only0.5 m criterion. This one
development case checks the missing Arm B integration before comparison;
stationary verification is its deliberate baseline behavior. The continuous
search objective remains represented by C/D. No numerical tuning or claim of
untouched conditions follows.

Use exposed nominal geometry and gain-half GESC profile from visible attempt03.
New case/run `v2_method_development_B_20260910_01`, seed26091003, exclusive root
`/home/mattb/Experiments/GESC-Gaussian/v2/development/20260910/stationary_integrated_01/`.
Preserve costs, source strengths, position, heading, Gaussian settings and all
control/safety owners. Select `recurrent_geometry_v3`, `stationary_v1`, legacy
`rolling_neighborhood_v1` verification selector, canonical recurrent diagnostic
and stationary recurrent request topics. Omit moving direction-policy/candidate
radius/epsilon overrides. Retain three3s verification windows, six available
pretrigger windows, verification12s, design5s, escape35s and no recenter.

## Existing runner correction before dispatch

Allow robust simulation recurrent/stationary through the current schema and
the named method-development event evaluator. Centered verification continues
to require rolling mode; fixed canonical topics, source-gap, state-gating,
profile/clock and original arrival restrictions remain. Add the new request
topic to the existing override allowlist. Preserve frozen M4v1–v9 definitions.
Use the exact recurrent diagnostic's history endpoint as private convergence
time; keep actual source/publication stamps and full persistence-origin check.
The existing stationary audit confirmation row also retains selected recurrent
branch, history/persistence support, counts, center, drift and confinement for
direct measurement in native analyzer tables; legacy rows remain unchanged.
The existing stationary fill request's own later publication remains its fill
correlation; do not force it equal to detector time. Retain unique chronological
convergence/fill association and all typed recorded authority checks.

Validate actual schema resolution, launch argv, event support/origin and
stationary local-fill association plus relevant moving/legacy regressions under
one120s cap. No new message or numerical change is needed. Correct any bounded
selection defect before the single-case freeze; record it and preserve failures.

## Finite acquisition and decision

Reuse the03 visible wrapper, subreaper_group_v3, strict owned cleanup and shared
daemon baseline. Select the new stationary interface environment, DISPLAY=:0,
localhost-only ROS domain201. No concurrent Gazebo/test/profile job. Keep
Stage A180s and post-recovery120s simulated bounds, recorder300s wall,
process340s wall, shutdown30s and root420s. A first qualifying post-recovery
arrival stops acquisition. These are finite allowances, not expected timing.
Save exact resolved argv, source/scenario hashes and prepared identity before
launch. Do not retry an unchanged failed version.

Require COMPLETE recording, valid selected stationary request/result authority,
owned cleanup, one local detector/verification/fill/recovery sequence and global
arrival for behavioral success. Report detection support/branch/persistence,
original admission, verification and fill timing, direct or assisted exit,
restored SEARCH, first arrival and all actual failures separately. A complete
behavioral failure is still informative development evidence. No GOAL_HOLD or
second ranking is required; safety stops remain enforced.

## One usable analysis after terminal cleanup

Invoke the existing `gesc_gaussian_bag_analysis.analyze_run` once in a new
exclusive output directory. Keep its one BagData decode and independent fresh
validator decode, before/after input hashes and atomic complete output. Bound
work by110s within118s SIGINT+2s kill/120s inclusive. A thin retained invocation
may count native reads and pin source/input identities; it may not replace the
analyzer or add another bag scan. The prior D-specific helper's moving exports
are inapplicable to B and must not be called.

Require complete usable analyzer outputs, `stationary_recurrent_pipeline`
status valid with no errors, explicit request/outcome/timing tables and actual
recording completeness. Zero requests or missing local/arrival events must be
reported honestly, never filled in. Direction normalization/reference jobs are
not part of this stationary integration check. Preserve all logs, elapsed time
and inputs even on failure. Working B analysis is a comparison prerequisite,
not permission to release twelve confirmation runs by itself.

After the result update live status/handoff, record exact evidence and checkpoint.
The new four-arm contract, analysis metrics/budget and development release gate
remain subsequent work. No matrix, physical/Pi/snapshot/V1 edit, commit or push.
