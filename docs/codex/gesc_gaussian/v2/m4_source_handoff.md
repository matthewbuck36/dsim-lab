# M4 execution/evaluation source handoff

Source milestone CLOSED_SOURCE_VALIDATION_PASS, 2026-09-09. Full M4/V2 remains
IN_PROGRESS. The two goals remain earlier trapping detection and better GESC
direction estimates while moving; neither performance target is established.

Read `m4_execution_evaluation_plan.md`, its active source clarifications,
`validation/m4_source_validation.md`, current `status.md`, and live Git state.
Q7 source and all older failed studies remain retained. The working branch is
`feature/gesc-gaussian-robustness-v2` at `3369cfc`; changes are saved but
uncommitted. No commit, push, physical snapshot/Pi/hardware or V1 action occurred.

The final composed source receipt under external `builds/m4_pilot_runtime_v1/`
is `root_final_composed_v2_receipt.json`, SHA256
`8bc1014a01241c1364d966f19a462c5bbf9576c99b55577faa2d17429f360207`:
767 unique passing tests, two actual CLI help checks,21 installed console
targets and606 identical source hashes. Original mixed-overlay invocation
failures are retained and explained in validation; historical expectations
and source stayed unchanged. Do not add overlapping test totals.

New subordinate owners are `plotting_scripts/m4_pilot.py`,
`scenario_runner/m4_scenario.py` and thin `v2/tools/{m4_workflow,m4_science_job,
run_m4,evaluate_m4}.py`. Existing runner/analyzer/shared geometry owners retain
control, recording, numerical and legacy responsibility. Reuse the Q2 then Q5
isolated overlays, appending `extremum-seeking/src` to their PYTHONPATH.

Next incomplete boundary: archive/checkpoint held source, then attempt the
single600s preparation with `tools/m4_workflow.py prepare`. It reserves the
exclusive external root `pilot/m4_pilot_v1`, verifies original primary/secondary
geometry receipt chains, derives exact secondary nominal/noise/delay topology
receipts using the unchanged numerical owner, and freezes all16 slots and
source/configuration/installed bindings. The wrapper and logs are external
`builds/m4_pilot_runtime_v1/run_preparation_v1.py` and `preparation_v1*`.
Read those receipts before acting; an existing root or partial attempt must
not be replaced or retried under this version.

Public `tools/run_m4.py --contract PATH` requires an explicit
`preflight/dispatch_release.json` binding prepared contract, source validation,
verified source archive and initial slots1–4. Do not infer release from this
handoff. Its dispatcher retains one absolute15300s suite deadline,720s
recording/900s inclusive case bounds, strict nested cleanup and no replacements.
After four complete safe development acquisitions and attempted finite science,
the separate one-time holdout release freezes those outcomes and the twelve
unstarted slots. Scientific failure/unavailability is retained; integrity or
source failure aborts later slots. First four Gazebo runs are visible.

Block science caps are120s labels,45s each C/D references and10s summary,
with20s total release/report work. Labels precede event joins, latency uses
actual decision publication, every one of192 direction targets is retained,
and missing error/stop/exposure attribution stays unavailable. Read the final
all16-slot report even after an abort. No extra qualification acquisitions,
post-freeze tuning, broader robustness claim or physical release is implied.
