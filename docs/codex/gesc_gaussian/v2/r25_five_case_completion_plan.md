# R25: finish the five unattempted comparison cases

ADOPTED after R24's61-check retained C11 validation and independent27-check review
passed. This is a new follow-up evidence envelope using existing runtime protocol
selection m4-pilot-v14. Original V14 remains CLOSED_INCOMPLETE and immutable.
No M4 version registration or production source change is needed.

## Population and prerequisite

Retain the original four V13 development recordings and their R22-qualified motion
view. Retain V14 slots5–10 unchanged and slot11 with a separately qualified R24
recording view. Preserve original identities, raw data, native statuses, failed
summary, C7 command-consistency failure and withheld qualified arrival. Reuse
the completed1–8 measurements through the checked cached supplement. C11 remains
a baseline behavioral failure, even when its recording is separately qualified.

Only five fresh acquisitions are allowed: slot12 D/noise seed26091153 and
slots13–16 A/B/C/D delay seed26091154. Keep the exact previously frozen scenario,
case/run IDs, suite, starts, geometry, cost/configuration, controller, clocks,
algorithm settings and launch argv. These IDs have never been executed. Before
release verify old slots12–16 are still UNSTARTED and no matching run directory
exists. New records live only under the exclusive external
`development/20260911/r25_five_case_completion_v1/` root. Keep old UNSTARTED receipts
unchanged; new receipts explicitly identify this follow-up and original reservation.

There are16 comparison recordings (4retained development,7retained confirmation,
5fresh follow-up), not16fresh runs. The corrected C11 view retains the original
INCOMPLETE status, metadata/completeness bytes and native no-arrival result as
provenance. Its effective recording eligibility derives only from R24's full
validation, original native/outer cleanup proofs, exact original scenario/launch
bindings and the reviewed R23 source correction. Do not relabel any native file.

Slots9–11 have no scientific metrics yet. Complete their first analysis and C11's
24-target direction product BEFORE releasing the five new acquisitions. Require
complete per-run measurements and complete reference products; baseline behavior
or direction failures remain results. Do not run new cases if this analysis fails.
Earlier working development and D nominal arrival/continuous/direction remain
prerequisites. All prior numerical direction products remain cached and unchanged.

## Existing owners and fixed source

A small external adapter may orchestrate the follow-up; it must call existing
owners rather than create nodes, recorders, validators or numerical pipelines.
Use run_m4.runner_command, validate_process_ownership, validate_launch_binding and
validate_case_result where applicable, and the existing run_scenario process and
cleanup owners. A separate execution view relocates only runs_root/summary paths;
it references the immutable original contract and is not a replacement V14 contract.
Do not call run_m4.dispatch or alter its strict16-row/reuse/release rules.

For the five new cases preserve the existing900s inclusive envelope,720s recorder,
45s shutdown grace, native plus outer and independent-inner cleanup, strict
subreaper_group_v3 and graceful signal strategy. Bind captured configuration and
geometry through m4_recorded_binding. Stop on integrity/process/source/recording
failure and keep remaining follow-up entries UNSTARTED; do not retry or replace.
Complete safe behavioral failures do not stop the comparison.

For scientific analysis use existing science_aliases, read_run_bag and
analyze_run_data with freeze_labels persisted before event inspection. Use
experiment_version=m4-pilot-v14 to preserve the selected recorded-arrival and
full-publication motion-pairing rules. Retain9–16 first-time metrics, all24 targets
for each C/D, and call evaluate_direction_targets through its unchanged model
and observed-phase GESC reference. No calculation or bag scan for1–8 is repeated.
Use aggregate_pilot and existing rendering for a separate final report, with the
effective and original acquisition populations/statuses both explicit. Preserve
all six latency pairs/twelve endpoints,48development/144confirmation targets,
wrong-fill/goal data, missing/censored entries and each D condition result.

Freeze adapter bytes, selected environment and source/input identities before
execution. Authenticate the V14-to-current production delta as exactly R23's
validator and lifecycle test with their before copies,187tests, independent review
and archive. Root phase-plan documentation changed after V14 closure; record
that separately. All other runtime/numerical/config/interface sources remain
unchanged. Retained raw hashes are inherited where available and file stats checked;
new recordings receive the normal native raw receipts. No post-freeze tuning.

## Finite execution and closeout

Use the existing clean R21 environment, single BLAS threads, domain201/localhost1,
DISPLAY:0 and batch rendering. One preparation/static review boundary precedes
the empirical jobs. New helpers receive syntax and independent static review;
production tests are already187PASS and are not repeated without a new change.

Retained9–11 prelude: one240s analysis allowance plus45s C reference and15s receipt
reserve, maximum300s inclusive. Save per-slot partial outputs and a terminal receipt.
The five-case continuation has a5400s inclusive ceiling: at most4500s acquisition,
the existing per-case/block first-analysis allowances, three45s C/D references
and bounded final reporting/cleanup. Every child is bounded within its parent;
no unbounded wait, retries or extra cases. Overall newly adopted empirical ceiling
is5700s inclusive, smaller than the old16-slot suite. Root records exact commands,
sole sessions, receipts and terminal ownership; observation timeout is never a
reason to restart a live handle.

Independent cached preparation/prelude and final result checks preserve the
original fixed failures. Close with exact16-slot comparison, source/data provenance,
all residual scientific limitations, current status/handoff and material checkpoint.
The original30% independent-onset latency remains unachieved when unavailable;
arrival or R10 response times cannot substitute for it. No physical/Pi/snapshot,
V1, commit or push. User authorization covers this simulation-only continuation;
no routine approval question is needed.
