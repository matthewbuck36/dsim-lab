# M3 implementation handoff — 2026-09-09 UTC

M3's source and bounded validation work is complete. The new moving pipeline
runs through the existing owners and passes its declared synthetic/ROS checks.
Detector/neighborhood calibration and direction research qualification remain
unachieved; the sixteen-run pilot has not started. This is not full-plan completion.

The supervisor owns authoritative SEARCH epochs and immutable candidate evidence.
Both detector modes bind actual selected source support to that epoch. Rolling
centroid and PDE-wrapper admission retain original receipts while source or
publication stamps await the receiver clock. Frame/origin faults, source gaps,
conflicts, revocations and expired receipts discard unsupported history without
rearming a confirmed epoch. Default/standalone numerical policies remain selectable.

The supervisor reuses the latest three qualifying observed revolutions, including
pretrigger support. Candidate-centered confinement, per-cycle/sector trajectory
comparison, exact raw minima and the existing median/MAD ranking precede the
information guard and classification. Raw evidence remains separate from augmented
direction confidence. Missing evidence ends at the original twelve-second deadline
and returns to SEARCH without a mandatory stopped acquisition sweep.

Existing controller gates authorize fresh selected-run GESC during VERIFY and
initial DESIGN; delayed escape/recenter commands cannot enter that path. Existing
readiness, safety, saturation, final GOAL and process-level final-zero behavior
remain. Redesign retains its prior escape ownership and original timeout.

The Gaussian owner prepares from detached immutable evidence in a bounded worker,
without allocating live IDs or changing the active objective. Validated ACTIVATE
commits the staged registry once; the combined result atomically updates the
existing composer. Cancellation before commit is terminal, accepted fills persist,
and late accepted results cannot return the robot to an old candidate. Supervisor
escape authorization waits for both objective and direction registry acknowledgements.

The existing recorder/analyzer preserve typed source, epoch, candidate and fill
transactions. Recorded raw minima, snapshot hashes, provenance and transaction
authority are checked. New interval metrics retain observed/valid denominators
and unavailable values; historical `convergence_time` remains unchanged. Epoch
latency is explicitly not an independent basin-entry latency measurement.

Exact commands, source boundaries, retained failed attempts and limitations are
in `validation/m3_validation.md` and its linked owner records. Principal results:

- Three-package build PASS20.7s; broad integration812PASS/1inherited quaternion
  warning53.36s. The warning concerns an unused inherited transform intermediate;
  checked values remain finite. Counts overlap with focused checks.
- Final Arm C admission68focused PASS0.43s and3actual PDE/detector DDS PASS1.14s;
  final recorder/analyzer temporal consistency7PASS2.58s. These bounded follow-ups
  postdate the812-check source boundary and are captured by the final archive.
- Supervisor51focused PASS; controller58focused including realSIGINT final zero
  plus19legacy; Gaussian51final checks including actual worker/composer DDS;
  recorder82checks at its preceding boundary. All are recorded with exact commands.
- Actual moving supervisor/worker/composer pipeline2DDS cases PASS7.86s: inactive
  preparation followed by accepted atomic activation and both acknowledgements;
  departure during pending computation cancels without any active registry change.

Transport fixtures use explicit synthetic trajectories, phase, raw costs and
neighborhoods. They do not measure natural convergence, calibrated neighborhoods,
angular accuracy or erroneous-fill rates in a field. No Gazebo, physical tree,
mount, transfer, device, hardware action, commit or push occurred during M3.

Recoverable boundary: branch `feature/gesc-gaussian-robustness-v2`, HEAD
`3369cfc83a64ff5d8354827fd5310caaf0c8e945`, with task-owned uncommitted edits.
V1 remains closed at1af67c6. The material archive is retained at
`/home/mattb/Experiments/GESC-Gaussian/v2/checkpoints/m3_boundary_v1/`, containing
source/evidence, binary patches, per-file/log hashes and exact Git state.
Its verified manifest digest is recorded in the live status after archive creation.

The next milestone must freeze a prospective qualification amendment before new
source/field work. `qualification_release_review.md` is a proposal, not a study
release: four primary-field observation runs, separate discovery/confirmation,
a finite neighborhood nomination and fresh typed same-trajectory references.
Resolve simulated observation duration and world/odom coordinate correspondence
before using nonzero starts with independent field labels. Preserve all earlier
failed/unavailable versions, the unopened historical holdouts, the pilot seeds,
and the approved four-arm/sixteen-run pilot and its outcome targets.


Archive verification receipt (appended after snapshot):142files,706691bytes,
54retained logs; manifest SHA256
`0e144f64d392741f5388a2eaef4c0b98dd1f9068669eb1de1b3533e644602920`.
Context validation, diff check and existing checkpoint tool passed. The archive
includes the handoff before this receipt and the exact completed source boundary.
