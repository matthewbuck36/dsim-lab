# R2/R3 single visible integrated development route

Status: ADOPTED, 2026-09-10. Simulation only; no dispatch until the root method
prototype, finite experiment hypothesis and final scenario are ready. This is
a distinct development identity, not another M4 comparison or old-slot retry.

Reuse `run_scenario` and its existing `record_run`, typed centroid/staged recovery,
subreaper group ownership, final-zero, full recording validator and cleanup
owners. Add only the named `v2_method_development_v1` suite to the runner's exact
ownership and centroid-evaluator route selections. Other suites, historical
identities, method selectors and public defaults retain their existing behavior.

Generate an external one-case scenario template by copying only the retained
V9 development D configuration, including its geometry/topology receipt,
GESC+Gaussian baseline controls and full fill/escape/ranked-goal behavior checks.
Use fresh suite/case/run/metadata/output identities, one nominal seed, GUI, and
the following prospective development budgets:

| Boundary | Limit |
| --- | --- |
| Stage A local recovery | 120 simulation seconds |
| Stage B after recovery | 60 simulation seconds |
| Recorder wall duration after readiness | 240 seconds |
| Scenario recorder process work | 280 wall seconds |
| Shutdown grace | 30 seconds |
| Recorder preflight | 45 seconds |
| Entire root attempt, including cleanup/report | 360 wall seconds |

Do not select `simulation_duration_sec`: the existing staged monitor can stop
earlier on a valid goal or stage deadline, and the recorder's fixed simulation
duration contract would classify that orderly earlier stop as incomplete. The
existing Stage A/B monitors bound simulated exposure to at most180 seconds while
the recorder and root wall limits bound stalled simulation. The existing
subreaper reserves shutdown grace plus15 seconds within the absolute deadline.
Finite failure is useful development evidence; the full goal contract is not
weakened just because this first development window is short.

For this suite only, after the ordinary shared daemon startup, require a bounded
exact daemon identity admission through the existing strict graph owner. A single
five-second child envelope covers DaemonNode name/namespace lookup, independent
native graph discovery and identity recheck. Repeat native discovery within that
same envelope if the exact daemon is initially absent. Preserve the entire
observed baseline; reject absent/changed/malformed identity before simulation.
No wildcard filtering, fabricated/cached baseline or broad process kill. This is
an observed startup admission, not an indefinite DDS discovery guarantee.

The known startup companion loss remains possible without a delivery redesign.
One bounded development attempt may assess its actual recording integrity through
the unchanged full-bag validator. Any missing objective/raw/augmented/provenance
join makes that attempt INCOMPLETE; do not waive startup rows or claim integrated
success from failed integrity. A40000-sample durability infrastructure change is
not required to perform this diagnostic and is not released by this plan.

Before production edits preserve scoped source externally under
`development/20260910/integrated_route_v1/`. Add focused fixtures for exact named
ownership admission, typed centroid routing, delayed/absent/changed daemon
visibility and five-second strict graph envelope. Run these plus existing typed
centroid and monitor progression tests under one90-second installed-environment
timeout; no actual graph, ROS node, bag, field model or simulation in source tests.
Retain exact argv/log/JUnit/source receipts. Root may replace the template's
method overrides with its separately validated prototype before freezing the
new attempt; this route imposes no new algorithm-selector restriction.
