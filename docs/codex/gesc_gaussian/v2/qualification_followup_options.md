# Read-only qualification follow-up options — 2026-09-09 UTC

These are planning findings, not a selected amendment or experiment release.
M2 remains active. No new Gazebo/hardware run is authorized by this note.

M1a's54 s horizon is the common comparison support for six times the largest
W=9 s, not an intrinsic floor for every configuration. W=3/6/9 requires18/36/54 s
of fresh eligible support. Retrospectively shortening the common horizon would
not solve the evidence gap: the longest retained positive residence is34.136 s,
and every M1a W=3 row fails its synthetic requirement. Preserve the closed grid,
labels, holes, censoring and missing sensitivity/latency estimates.

One prospective option is a separate observation-only simulation study with
the existing GESC and recorder. Record detector outputs in shadow while
suppressing detector-triggered acquisition/fill/escape transitions during the
observation interval; retain all safety stops. Independently choose starts from
accepted geometry before seeing detector output. Let trajectories evolve
naturally; do not hold them inside the annulus, fill its holes, join interrupted
episodes or run until success. A possible finite design is two geometries,
two independently chosen starts and two new seeds (eight runs), each120 s after
readiness with240 s wall cap. This is an **unselected additional-study option**,
requiring a prospective amendment/release decision before any action. Failure
to obtain continuous positive exposure remains an exposure failure.

Any later parameter nomination needs an a priori discovery/confirmation split,
an independently fixed labeling rule and a fresh finite candidate set or
analytic nomination rule. Do not rerun the closed36-grid as if it were new or
call tied synthetic radii calibrated. Detector confinement radius alone does
not establish M3 cycle-centroid/phase/trajectory/cost comparability.

M3 source/interface/race/controller tests can later use clearly synthetic
neighborhood fixtures while research qualification remains unresolved. The
approved four visible M4 development arms remain matched behavior comparisons;
they cannot silently become shadow observation runs with different starts.
Early interventions can also truncate SEARCH support, making those comparisons
a poor replacement for a prospective exposure study. Closed-loop sustained-entry
latency labels and censoring must be fixed explicitly rather than importing or
discarding the historical54 s comparison rule without justification.

No option has been selected, no threshold changed, and no experiment run.
