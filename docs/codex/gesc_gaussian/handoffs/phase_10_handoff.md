# Phase 10 Handoff — V1 Final Project Report and Documentation Closeout

## 2026-09-08 addendum — root environment guidance complete

The user reopened V1 for final root documentation. The simulation README and
AGENTS now identify its clock/mode boundary and link the source-backed
`docs/environment_parameters.md`. Root README/AGENTS files were installed in
the local physical snapshot and the unmounted local `tb3-pi` directory, with
exact versioned copies in `docs/environment_guides/`. No real-Pi files were
accessed or written. Mounted SSHFS hides the local mount-point instructions;
use their tracked copies explicitly when working on a mounted tree.

Verification passed: 55 source/copy/structure assertions, 25 local links,
12 Markdown files, and the existing 16-test clock suite independently against
each source tree. Context/required-document and diff checks passed. See
[`environment_guidance_validation.md`](../validation/environment_guidance_validation.md)
for exact commands, scope, local paths, and hashes. The current Phase 10
checkpoint records this precommit documentation boundary on base `dca77ba`.
The user subsequently authorized committing these files and closing V1 with
the exact subject `docs: close V1 with environment guidance`. The containing
commit is the final V1 branch change and supersedes `dca77ba`; verify a clean
worktree after creation. Do not amend or extend it without an explicit user
reopening. No push or new branch was requested.

Resolve the final commit without embedding its own hash in its contents:

```bash
git log -1 --format=%H --grep='^docs: close V1 with environment guidance$' feature/gesc-gaussian-robustness-v1
```

No runtime parameters, shared algorithms, legacy behavior, frozen report,
experimental results, or physical readiness claim changed. No required work
remains for this bounded amendment. The original dated closeout below is
preserved as history, including its older Git and physical-mount state.

## Objective completed

Phase 10 completed the documentation-only closeout of the
`feature/gesc-gaussian-robustness-v1` evidence line. The canonical deliverable
is the LaTeX-typeset [FINAL_PROJECT_REPORT_V1.pdf](../FINAL_PROJECT_REPORT_V1.pdf),
with [FINAL_PROJECT_REPORT_V1.tex](../FINAL_PROJECT_REPORT_V1.tex) as its
authoritative source and [FINAL_PROJECT_REPORT_V1.md](../FINAL_PROJECT_REPORT_V1.md)
as its auditable repository companion. It is a self-contained 20-section,
thesis-facing account of the complete implementation package,
current architecture/mathematics/interfaces/parameters, Phases 00–10, every
material Phase 08 experiment/version, all Phase 09 milestones and physical
attempts, recording/analysis products, tests, commands, limitations,
reproducibility, and future V2 boundary.

The report's supported conclusion is deliberately narrow. V1 demonstrated
selected two-basin behavior, including v8.10 primary `11/11`, v8.11 secondary
`6/6`, v8.12 visible `14/14`, and one physical behavioral success. It did not
pass broad simulation readiness, broad physical readiness, or a complete
second-extremum physical acceptance. The v8.12 varied case remained `13/14`
with three cases withheld; the eighth physical run stopped before a second
convergence/`GOAL_HOLD` and remained `61/62` complete because of a retained
cross-topic shutdown-order false negative.

## Repository state

- Repository: `/home/mattb/dsim-lab`
- Branch: `feature/gesc-gaussian-robustness-v1`
- Parent/base HEAD: `e3dd0ef5cfa5a79ee8bed3017bf844f4f269add7`
- Upstream relation before the closeout commit: ahead of
  `origin/feature/gesc-gaussian-robustness-v1` by 195 commits.
- Commit authorization: after the documentation gates passed, the user
  authorized one local commit with subject
  `docs(phase10): close GESC Gaussian V1 report and evidence`. The V1 boundary
  is the commit containing this handoff with that exact subject. Resolve its
  full ID with `git log -1 --format=%H -- docs/codex/gesc_gaussian/handoffs/phase_10_handoff.md`; the object ID is not
  self-embedded because doing so would change it.
- Commit scope: exactly the 20 modified and nine created documentation files
  listed here; no runtime/source/interface/config or prior-phase evidence file.
- Live status final state: `COMPLETE` after the final checks recorded below.
- Final checkpoint: generated by `checkpoint_phase.sh 10` after closeout; it is
  a precommit material-boundary snapshot. The helper records untracked paths
  but its tracked-diff hash cannot cover their file contents.
- Physical boundary: `/home/mattb/physical_TB3_files_snapshot` was inspected
  read-only. The unexpectedly mounted `/home/mattb/tb3-pi` was not listed,
  read, written, transferred, unmounted, remounted, or otherwise accessed.

## Files changed

Created:

- `docs/codex/gesc_gaussian/FINAL_PROJECT_REPORT_V1.md`
- `docs/codex/gesc_gaussian/FINAL_PROJECT_REPORT_V1.tex`
- `docs/codex/gesc_gaussian/FINAL_PROJECT_REPORT_V1.pdf`
- `docs/codex/gesc_gaussian/plans/phase_10_plan.md`
- `docs/codex/gesc_gaussian/status/phase_10_status.md`
- `docs/codex/gesc_gaussian/checkpoints/phase_10_checkpoint.txt`
- `docs/codex/gesc_gaussian/validation/phase_10_report_coverage.tsv`
- `docs/codex/gesc_gaussian/validation/phase_10_documentation_validation.md`
- `docs/codex/gesc_gaussian/handoffs/phase_10_handoff.md`

Updated active navigation/reference owners:

- `README.md`
- `EXPERIMENT_STORAGE.md`
- nine implementation-package specifications/navigation/prompts declared by
  the Plan;
- `docs/codex/gesc_gaussian/{implementation_sequence,interface_map,topic_dictionary,recording_runs,test_commands}.md`;
- `ros2_ws/src/{ros_esc,ros_esc_interfaces,turtlebot3_rotating_sensor}/README.md`;
- `writing/README.md`.

Prior Plans, statuses, checkpoints, handoffs, validation/failure reports,
manifests, retained runs, bags, snapshot files, runtime source, launch files,
configs, and interfaces remain unmodified.

## Public interfaces and parameters

No ROS topic, message, service, action, parameter, default, rate, sign, unit,
launch argument, CLI option, package dependency, runtime owner, or algorithm
behavior changed. Phase 10 added documentation interfaces only:

- the canonical V1 PDF, authoritative LaTeX source, and Markdown audit path;
- the coverage TSV;
- this validation/status/checkpoint/handoff chain.

The report derives node/direct defaults and selected v8.12/physical overrides
from live source, scenario YAML, launch XML, installed interfaces/entry points,
and the read-only physical snapshot. It preserves physical sensor `+V`, raw
cost `-V`, `/odom` as algorithm pose, Vicon as passive evaluation-only
odometry, `controller_node` as sole `/cmd_vel` owner, `record_run` as sole
recorder/readiness/rotation/final-zero owner, and `validate_run` as sole
validator.

## Behavior and evidence documented

- All 57 unique implementation-package manifest paths are classified.
- Phase 00–07.5 implementation/test outcomes and historical missing-status/
  checkpoint boundary are recorded.
- Phase 08's 12 Plans, 9 handoffs, 97 validation artifacts, 56 current local
  run roots, failures/corrections/early stops/withheld cases, selected results,
  and failed broad-readiness outcome are indexed.
- Phase 09's four lifecycle and 21 validation artifacts, snapshot/transfer/
  parity process, M0–M8L chronology, unnumbered commissioning failure, eight
  numbered attempts, check-only results, repairs, final architecture, exact run
  paths, and narrow eighth-run result are indexed.
- Recording, shutdown, CSV/plot/summary products, external storage, operating
  commands, compatibility, reproducibility, threats/nonclaims, Git chronology,
  glossary, and the future V2 research boundary are explicit.

## Backward compatibility

`legacy` remains the direct-launch default. Robust V1 remains explicit opt-in
through `robust_gaussian_v1`. Five compatibility messages, legacy topics/
arrays/configs/wrappers, and Heavy-Ball source/history remain intact. Active
entry points label Heavy-Ball material historical/archive or compatibility and
route current work to GESC + Gaussian. Failed and superseded evidence was not
rewritten or deleted.

## Tests and documentation checks

Final aggregate results are recorded in
[phase_10_documentation_validation.md](../validation/phase_10_documentation_validation.md)
and the appended Phase 10 section of [test_commands.md](../test_commands.md).

- Phase context and required Phase 00 documents: PASS.
- Coverage: PASS — 174 rows; exact 57/57 manifest coverage; 44 explicit
  simulation results; eight physical attempts; 20 required report headings;
  no empty required cells or uncovered phase artifact.
- Markdown/local links/anchors/fences/Mermaid: PASS; exact final totals are in
  the validation record.
- LaTeX/PDF: PASS — Tectonic 0.17.0 compiled the 39-page letter PDF with zero
  errors, missing characters, or overfull boxes; all 16 fonts are embedded;
  six vector figures and representative pages passed text and visual review.
- XML/YAML/JSON/Bash structure and 51 selected-override assertions: PASS.
- Six installed robust messages, six evidence entry points, five CLI help
  routes, and six queried launch arguments: PASS.
- Current shared canonical/snapshot parity: 27/27 PASS; local snapshot 347
  regular files and zero symlinks.
- Claim-specific review: PASS; selected denominators, failures, withheld cases,
  Vicon boundary, manual stop, and no-V2 statements are present; forbidden
  positive-readiness/control claims are absent.
- Changed-path scope, prior-evidence immutability, `git diff --check`, staged
  state, and branch: PASS.

No ROS build, unit/regression suite, Gazebo launch, simulation matrix,
physical check-only, physical run, or full inherited lint suite was required or
executed. The phase changed documentation only and relied on current source/
installed-interface inspection plus retained authoritative runtime evidence.

## Contradictions and corrections

### Level A

None. No unresolved sign/unit, owner, simulation/physical parity,
`/odom`/Vicon, safety, evidence-integrity, or authorization conflict remained.

### Level B

- The first status initialization attempt used the stale `dsim_ws/src/...`
  prefix and failed before state change; repository-root package tools were
  resolved and used.
- Current `findmnt` showed the physical SSHFS mount present despite its earlier
  handoff being unmounted. Phase 10 treated it as out of scope and never
  accessed it.
- Older active navigation stopped at M8C–M8E or had stale M8L text. Current
  addenda point to the later eighth-run validation without rewriting history.
- Six historical README file/image links were root-relative; they were
  corrected to existing repository-relative targets.
- One source assertion initially used prose controller field names rather than
  the actual JSON keys. The failed diagnostic made no edit; the key-exact
  assertion passed.
- One report-table prototype overcounted repeated row labels outside its target
  sections. A section-scoped checker replaced it and passed exact row counts.

### Level C

No Phase 10 acceptance failure. Historical V1 Level C failures remain failed
and are a central part of the report. Pi-side run roots were not live-verified;
they are explicitly quoted from retained repository evidence.

## Known limitations and unresolved failures

- Broad simulation robustness failed; no 70-unique denominator or
  simulation-ready tag exists.
- Broad physical robustness/readiness and repeatability are unproven.
- The eighth run did not reach a second confirmation/classification or
  `GOAL_HOLD`; its formal completeness remains 61/62.
- Counted-candidate V1 assumes two sources; arbitrary source count/layout,
  three-light, walls/obstacles, reflections/shadows, broad noise/delay, and
  calibrated sim-to-real radiometry remain unestablished.
- Inherited lint/style debt remains substantial.
- High-volume evidence lives outside Git and may not be locally available on a
  different machine.
- The closeout commit ID is intentionally resolved from Git rather than
  self-embedded in the commit it identifies.

## Decisions made

- The `FINAL_PROJECT_REPORT_V1.pdf`/`.tex`/`.md` set is the immutable-named V1
  report.
- Formal, behavioral, infrastructure, selected, and broad denominators remain
  separate; incompatible versions are never pooled.
- V1 evidence is preserved even when V2 later improves the system.
- No M8M validator change, runtime correction, rerun, V2 design choice, or V2
  branch was created during Phase 10.

## Exact next phase

The V1 closeout commit has been authorized with the exact subject below. Do not
create `feature/gesc-gaussian-robustness-v2` during this commit step. When V2 is
separately authorized, create it **from the containing closeout commit** and
start with a new evidence-backed Plan; do not branch from `e3dd0ef` or edit V1
results in place.

## Recommended commit message

```text
docs(phase10): close GESC Gaussian V1 report and evidence
```
