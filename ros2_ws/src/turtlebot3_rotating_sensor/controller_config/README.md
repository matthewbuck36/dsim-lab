# Description

This folder contains the configuration .yaml file that is used by Gazebo Ros2 Control.
This specifies the revolute joint where the rotating sensor frame attaches to the
Turtlebot vehicle, and specifies the state variables of interest. This config file
is referenced in the Turltlebot's URDF file.