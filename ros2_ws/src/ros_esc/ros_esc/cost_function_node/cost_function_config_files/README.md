# Description:

This folder contains examples of cost function configuration files.
The user must specify a "CostFunction" object in every configuration file.
The user must also specify a "Noise" object in every configuration file
which adds noise to the cost value signal. The user should add the necessary
parameters to instantiate these objects in the configuration file.
